import React from 'react'
import { Icon, IconProps } from '../Icon'

export function audioBook01Icon({ iconName = "audio-book01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}