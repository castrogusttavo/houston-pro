import React from 'react';
import { Icon, IconProps } from '../Icon';

export const keyframes-multipleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="keyframes-multipleicon" />
);