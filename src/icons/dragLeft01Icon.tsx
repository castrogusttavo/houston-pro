import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragLeft01Icon({ iconName = "drag-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}