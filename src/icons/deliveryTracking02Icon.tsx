import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryTracking02Icon({ iconName = "delivery-tracking02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}