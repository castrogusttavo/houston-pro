import React from 'react'
import { Icon, IconProps } from '../Icon'

export function maximize04Icon({ iconName = "maximize04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}