import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingDown01Icon({ iconName = "hand-pointing-down01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}