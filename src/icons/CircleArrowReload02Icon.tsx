import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowReload02Icon({
  iconName = 'circle-arrow-reload02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
