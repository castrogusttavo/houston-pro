import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-down01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-down01icon" />
);