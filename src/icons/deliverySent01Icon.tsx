import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliverySent01Icon({ iconName = "delivery-sent01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}