import React from 'react'
import { Icon, IconProps } from '../Icon'

export function babyBoyDressIcon({ iconName = "baby-boy-dress", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}