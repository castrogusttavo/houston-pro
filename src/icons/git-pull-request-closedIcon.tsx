import React from 'react';
import { Icon, IconProps } from '../Icon';

export const git-pull-request-closedIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="git-pull-request-closedicon" />
);