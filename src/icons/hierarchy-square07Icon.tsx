import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hierarchy-square07Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hierarchy-square07icon" />
);