import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderBottom01Icon({ iconName = "border-bottom01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}