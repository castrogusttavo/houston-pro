import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowExpand01Icon({ iconName = "circle-arrow-expand01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}