import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageDownload02Icon({ iconName = "message-download02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}