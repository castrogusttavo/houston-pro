import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraOff02Icon({ iconName = "camera-off02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}