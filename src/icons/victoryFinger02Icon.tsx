import React from 'react'
import { Icon, IconProps } from '../Icon'

export function victoryFinger02Icon({ iconName = "victory-finger02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}