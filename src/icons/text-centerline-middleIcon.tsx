import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-centerline-middleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-centerline-middleicon" />
);