import React from 'react';
import { Icon, IconProps } from '../Icon';

export const computer-programming01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="computer-programming01icon" />
);