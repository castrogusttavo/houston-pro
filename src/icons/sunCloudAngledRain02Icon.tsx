import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudAngledRain02Icon({ iconName = "sun-cloud-angled-rain02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}