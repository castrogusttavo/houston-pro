import React from 'react'
import { Icon, IconProps } from '../Icon'

export function keyframesMultipleAddIcon({ iconName = "keyframes-multiple-add", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}