import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NotificationBlock03Icon({ iconName = "notification-block03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}