import React from 'react'
import { Icon, IconProps } from '../Icon'

export function thirdBrecketSquareIcon({ iconName = "third-brecket-square", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}