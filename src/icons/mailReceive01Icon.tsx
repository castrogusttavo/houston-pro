import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailReceive01Icon({ iconName = "mail-receive01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}