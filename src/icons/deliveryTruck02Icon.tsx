import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryTruck02Icon({ iconName = "delivery-truck02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}