import React from 'react';
import { Icon, IconProps } from '../Icon';

export const pathfinder-excludeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="pathfinder-excludeicon" />
);