import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft01Icon({ iconName = "pointing-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}