import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarSetting01Icon({
  iconName = 'calendar-setting01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
