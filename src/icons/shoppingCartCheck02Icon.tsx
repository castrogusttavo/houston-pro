import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingCartCheck02Icon({ iconName = "shopping-cart-check02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}