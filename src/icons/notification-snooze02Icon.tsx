import React from 'react';
import { Icon, IconProps } from '../Icon';

export const notification-snooze02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="notification-snooze02icon" />
);