import React from 'react'
import { Icon, IconProps } from '../Icon'

export function downloadCircle02Icon({ iconName = "download-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}