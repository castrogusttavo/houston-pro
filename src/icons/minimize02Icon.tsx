import React from 'react'
import { Icon, IconProps } from '../Icon'

export function minimize02Icon({ iconName = "minimize02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}