import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowExpand01Icon({ iconName = "arrow-expand01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}