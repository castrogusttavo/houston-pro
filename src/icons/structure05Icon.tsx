import React from 'react'
import { Icon, IconProps } from '../Icon'

export function structure05Icon({ iconName = "structure05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}