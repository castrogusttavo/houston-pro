import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hierarchy-square06Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hierarchy-square06icon" />
);