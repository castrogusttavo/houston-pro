import React from 'react';
import { Icon, IconProps } from '../Icon';

export const internet-antenna02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="internet-antenna02icon" />
);