import React from 'react'
import { Icon, IconProps } from '../Icon'

export function soilMoistureGlobalIcon({ iconName = "soil-moisture-global", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}