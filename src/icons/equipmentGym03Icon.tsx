import React from 'react'
import { Icon, IconProps } from '../Icon'

export function equipmentGym03Icon({ iconName = "equipment-gym03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}