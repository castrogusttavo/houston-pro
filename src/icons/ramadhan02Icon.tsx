import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ramadhan02Icon({ iconName = "ramadhan02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}