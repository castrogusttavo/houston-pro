import React from 'react'
import { Icon, IconProps } from '../Icon'

export function batteryMedium02Icon({ iconName = "battery-medium02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}