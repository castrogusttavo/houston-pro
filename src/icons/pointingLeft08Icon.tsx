import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft08Icon({ iconName = "pointing-left08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}