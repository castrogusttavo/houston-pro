import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationStar02Icon({ iconName = "location-star02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}