import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-fast-wind01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-fast-wind01icon" />
);