import React from 'react';
import { Icon, IconProps } from '../Icon';

export const rotate-bottom-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="rotate-bottom-lefticon" />
);