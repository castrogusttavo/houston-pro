import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PackageDimensions02Icon({
  iconName = 'package-dimensions-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
