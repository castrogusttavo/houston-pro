import React from 'react'
import { Icon, IconProps } from '../Icon'

export function geometricShapes01Icon({ iconName = "geometric-shapes01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}