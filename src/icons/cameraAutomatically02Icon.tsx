import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraAutomatically02Icon({ iconName = "camera-automatically02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}