import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-lock-minus02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-lock-minus02icon" />
);