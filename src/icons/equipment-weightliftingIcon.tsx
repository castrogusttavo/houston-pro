import React from 'react';
import { Icon, IconProps } from '../Icon';

export const equipment-weightliftingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="equipment-weightliftingicon" />
);