import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorEdit02Icon({ iconName = "cursor-edit02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}