import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderAll02Icon({ iconName = "border-all02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}