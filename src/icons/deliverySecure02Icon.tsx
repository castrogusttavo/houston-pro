import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliverySecure02Icon({ iconName = "delivery-secure02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}