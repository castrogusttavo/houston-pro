import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrangeByNumbers91Icon({
  iconName = 'arrange-by-numbers91',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
