import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TableLamp02Icon({
  iconName = 'table-lamp02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
