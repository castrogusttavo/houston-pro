import React from 'react'
import { Icon, IconProps } from '../Icon'

export function minusPlusSquare02Icon({ iconName = "minus-plus-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}