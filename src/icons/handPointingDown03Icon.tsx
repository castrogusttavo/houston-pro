import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingDown03Icon({ iconName = "hand-pointing-down03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}