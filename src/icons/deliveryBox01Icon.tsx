import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryBox01Icon({ iconName = "delivery-box01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}