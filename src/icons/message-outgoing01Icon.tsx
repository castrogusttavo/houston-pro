import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-outgoing01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-outgoing01icon" />
);