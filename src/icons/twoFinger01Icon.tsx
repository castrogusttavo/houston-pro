import React from 'react'
import { Icon, IconProps } from '../Icon'

export function twoFinger01Icon({ iconName = "two-finger01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}