import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowMoveDownLeftIcon({ iconName = "circle-arrow-move-down-left", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}