import React from 'react'
import { Icon, IconProps } from '../Icon'

export function taskDaily02Icon({ iconName = "task-daily02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}