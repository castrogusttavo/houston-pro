import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapPinpoint01Icon({ iconName = "map-pinpoint01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}