import React from 'react'
import { Icon, IconProps } from '../Icon'

export function IceCream01Icon({ iconName = "ice-cream01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}