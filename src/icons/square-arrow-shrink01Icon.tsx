import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-shrink01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-shrink01icon" />
);