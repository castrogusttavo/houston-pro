import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowMoveUpLeftIcon({ iconName = "circle-arrow-move-up-left", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}