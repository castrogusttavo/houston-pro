import React from 'react';
import { Icon, IconProps } from '../Icon';

export const picture-in-picture-exitIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="picture-in-picture-exiticon" />
);