import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowLeft02Icon({ iconName = "arrow-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}