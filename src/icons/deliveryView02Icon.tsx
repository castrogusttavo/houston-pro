import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryView02Icon({ iconName = "delivery-view02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}