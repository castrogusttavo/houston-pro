import React from 'react';
import { Icon, IconProps } from '../Icon';

export const finger-print-removeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="finger-print-removeicon" />
);