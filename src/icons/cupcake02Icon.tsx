import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cupcake02Icon({ iconName = "cupcake02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}