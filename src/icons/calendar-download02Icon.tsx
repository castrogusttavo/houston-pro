import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-download02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-download02icon" />
);