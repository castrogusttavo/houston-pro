import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UploadSquare01Icon({
  iconName = 'upload-square01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
