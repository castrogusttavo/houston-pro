import React from 'react';
import { Icon, IconProps } from '../Icon';

export const analysis-text-linkIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="analysis-text-linkicon" />
);