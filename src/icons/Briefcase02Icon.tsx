import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase02Icon({ iconName = "briefcase02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}