import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDownRight01Icon({ iconName = "arrow-down-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}