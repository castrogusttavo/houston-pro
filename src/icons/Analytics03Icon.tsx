import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Analytics03Icon({
  iconName = 'analytics-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
