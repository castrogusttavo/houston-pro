import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-reload-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-reload-horizontalicon" />
);