import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingLeft02Icon({ iconName = "hand-pointing-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}