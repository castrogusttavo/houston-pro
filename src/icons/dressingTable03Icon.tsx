import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dressingTable03Icon({ iconName = "dressing-table03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}