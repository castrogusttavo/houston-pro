import React from 'react'
import { Icon, IconProps } from '../Icon'

export function walletAdd02Icon({ iconName = "wallet-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}