import React from 'react';
import { Icon, IconProps } from '../Icon';

export const greater-than-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="greater-than-circleicon" />
);