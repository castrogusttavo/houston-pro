import React from 'react'
import { Icon, IconProps } from '../Icon'

export function discount01Icon({ iconName = "discount01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}