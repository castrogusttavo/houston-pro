import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiSearch02Icon({
  iconName = 'ai-search-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
