import React from 'react';
import { Icon, IconProps } from '../Icon';

export const chart-breakout-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="chart-breakout-squareicon" />
);