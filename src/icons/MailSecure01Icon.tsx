import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailSecure01Icon({ iconName = "mail-secure01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}