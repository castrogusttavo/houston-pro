import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowDown02Icon({ iconName = "circle-arrow-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}