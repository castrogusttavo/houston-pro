import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeLeft03Icon({ iconName = "swipe-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}