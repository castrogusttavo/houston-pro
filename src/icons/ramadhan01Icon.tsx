import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ramadhan01Icon({ iconName = "ramadhan01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}