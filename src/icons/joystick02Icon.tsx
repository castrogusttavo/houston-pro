import React from 'react'
import { Icon, IconProps } from '../Icon'

export function joystick02Icon({ iconName = "joystick02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}