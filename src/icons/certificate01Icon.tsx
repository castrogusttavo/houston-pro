import React from 'react'
import { Icon, IconProps } from '../Icon'

export function certificate01Icon({ iconName = "certificate01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}