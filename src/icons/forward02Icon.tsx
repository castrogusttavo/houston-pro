import React from 'react'
import { Icon, IconProps } from '../Icon'

export function forward02Icon({ iconName = "forward02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}