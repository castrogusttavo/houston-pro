import React from 'react'
import { Icon, IconProps } from '../Icon'

export function imageRemove02Icon({ iconName = "image-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}