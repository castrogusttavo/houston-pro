import React from 'react'
import { Icon, IconProps } from '../Icon'

export function asteroid02Icon({ iconName = "asteroid02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}