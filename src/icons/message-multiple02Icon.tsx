import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-multiple02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-multiple02icon" />
);