import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowDiagonal01Icon({ iconName = "circle-arrow-diagonal01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}