import React from 'react'
import { Icon, IconProps } from '../Icon'

export function horizonalScrollPointIcon({ iconName = "horizonal-scroll-point", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}