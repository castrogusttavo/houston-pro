import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarLove02Icon({ iconName = "calendar-love02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}