import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare06Icon({ iconName = "workflow-square06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}