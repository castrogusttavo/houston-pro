import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hierarchy-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hierarchy-square02icon" />
);