import React from 'react';
import { Icon, IconProps } from '../Icon';

export const camera-microphone02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="camera-microphone02icon" />
);