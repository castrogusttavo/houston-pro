import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mouseScroll02Icon({ iconName = "mouse-scroll02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}