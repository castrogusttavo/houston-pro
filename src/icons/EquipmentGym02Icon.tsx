import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EquipmentGym02Icon({
  iconName = 'equipment-gym02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
