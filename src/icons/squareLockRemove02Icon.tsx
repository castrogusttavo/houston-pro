import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareLockRemove02Icon({ iconName = "square-lock-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}