import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorMove02Icon({ iconName = "cursor-move02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}