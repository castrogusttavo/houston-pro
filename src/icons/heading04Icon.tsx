import React from 'react'
import { Icon, IconProps } from '../Icon'

export function heading04Icon({ iconName = "heading04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}