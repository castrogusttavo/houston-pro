import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudSlowWind01Icon({
  iconName = 'sun-cloud-slow-wind01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
