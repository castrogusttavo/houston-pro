import React from 'react'
import { Icon, IconProps } from '../Icon'

export function checkmarkSquare03Icon({ iconName = "checkmark-square03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}