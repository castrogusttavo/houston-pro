import React from 'react'
import { Icon, IconProps } from '../Icon'

export function candelier01Icon({ iconName = "candelier01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}