import React from 'react'
import { Icon, IconProps } from '../Icon'

export function goForward15SecIcon({ iconName = "go-forward15-sec", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}