import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SignalMedium01Icon({
  iconName = 'signal-medium01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
