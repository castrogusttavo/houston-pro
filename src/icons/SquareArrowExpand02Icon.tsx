import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowExpand02Icon({
  iconName = 'square-arrow-expand02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
