import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailBlock02Icon({ iconName = "mail-block02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}