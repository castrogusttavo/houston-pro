import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyNotFound04Icon({ iconName = "money-not-found04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}