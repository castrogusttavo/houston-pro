import React from 'react'
import { Icon, IconProps } from '../Icon'

export function internetAntenna02Icon({ iconName = "internet-antenna02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}