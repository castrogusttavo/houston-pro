import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockAdd01Icon({ iconName = "circle-lock-add01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}