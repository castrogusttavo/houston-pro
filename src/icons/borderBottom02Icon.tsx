import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderBottom02Icon({ iconName = "border-bottom02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}