import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase08Icon({ iconName = "briefcase08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}