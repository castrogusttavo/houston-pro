import React from 'react'
import { Icon, IconProps } from '../Icon'

export function commentRemove02Icon({ iconName = "comment-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}