import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowShrink01Icon({
  iconName = 'arrow-shrink01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
