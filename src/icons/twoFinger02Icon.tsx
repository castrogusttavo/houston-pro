import React from 'react'
import { Icon, IconProps } from '../Icon'

export function twoFinger02Icon({ iconName = "two-finger02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}