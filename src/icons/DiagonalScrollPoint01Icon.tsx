import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiagonalScrollPoint01Icon({
  iconName = 'diagonal-scroll-point01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
