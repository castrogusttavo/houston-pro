import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageDelay02Icon({ iconName = "message-delay02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}