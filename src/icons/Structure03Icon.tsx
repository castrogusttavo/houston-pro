import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Structure03Icon({
  iconName = 'structure03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
