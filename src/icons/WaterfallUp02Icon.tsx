import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WaterfallUp02Icon({ iconName = "waterfall-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}