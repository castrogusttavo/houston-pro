import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailSend02Icon({ iconName = "mail-send02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}