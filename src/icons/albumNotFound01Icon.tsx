import React from 'react'
import { Icon, IconProps } from '../Icon'

export function albumNotFound01Icon({ iconName = "album-not-found01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}