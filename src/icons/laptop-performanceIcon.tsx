import React from 'react';
import { Icon, IconProps } from '../Icon';

export const laptop-performanceIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="laptop-performanceicon" />
);