import React from 'react';
import { Icon, IconProps } from '../Icon';

export const approximately-equal-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="approximately-equal-circleicon" />
);