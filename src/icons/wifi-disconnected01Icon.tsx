import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wifi-disconnected01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wifi-disconnected01icon" />
);