import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-setting02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-setting02icon" />
);