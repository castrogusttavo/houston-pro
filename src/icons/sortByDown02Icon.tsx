import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sortByDown02Icon({ iconName = "sort-by-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}