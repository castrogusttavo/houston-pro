import React from 'react'
import { Icon, IconProps } from '../Icon'

export function backward02Icon({ iconName = "backward02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}