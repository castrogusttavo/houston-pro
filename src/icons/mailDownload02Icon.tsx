import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailDownload02Icon({ iconName = "mail-download02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}