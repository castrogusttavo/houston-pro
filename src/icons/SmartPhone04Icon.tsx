import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SmartPhone04Icon({ iconName = "smart-phone04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}