import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-add01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-add01icon" />
);