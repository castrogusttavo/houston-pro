import React from 'react'
import { Icon, IconProps } from '../Icon'

export function victoryFinger01Icon({ iconName = "victory-finger01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}