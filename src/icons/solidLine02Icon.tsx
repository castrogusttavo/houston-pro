import React from 'react'
import { Icon, IconProps } from '../Icon'

export function solidLine02Icon({ iconName = "solid-line02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}