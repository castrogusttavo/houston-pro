import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-vertical-alignmentIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-vertical-alignmenticon" />
);