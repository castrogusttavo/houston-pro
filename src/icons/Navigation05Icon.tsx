import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Navigation05Icon({
  iconName = 'navigation-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
