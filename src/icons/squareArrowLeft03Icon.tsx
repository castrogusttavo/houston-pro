import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowLeft03Icon({ iconName = "square-arrow-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}