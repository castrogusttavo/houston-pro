import React from 'react'
import { Icon, IconProps } from '../Icon'

export function archive02Icon({ iconName = "archive02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}