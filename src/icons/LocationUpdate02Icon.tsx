import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationUpdate02Icon({
  iconName = 'location-update02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
