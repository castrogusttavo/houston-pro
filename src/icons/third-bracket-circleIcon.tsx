import React from 'react';
import { Icon, IconProps } from '../Icon';

export const third-bracket-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="third-bracket-circleicon" />
);