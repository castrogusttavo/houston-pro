import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeDown03Icon({ iconName = "swipe-down03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}