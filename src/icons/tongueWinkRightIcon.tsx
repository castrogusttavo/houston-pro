import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tongueWinkRightIcon({ iconName = "tongue-wink-right", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}