import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-hailstoneIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-hailstoneicon" />
);