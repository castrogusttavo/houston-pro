import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageLock02Icon({ iconName = "message-lock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}