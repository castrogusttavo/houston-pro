import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tickDouble03Icon({ iconName = "tick-double03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}