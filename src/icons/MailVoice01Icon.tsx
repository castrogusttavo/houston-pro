import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailVoice01Icon({ iconName = "mail-voice01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}