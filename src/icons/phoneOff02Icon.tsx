import React from 'react'
import { Icon, IconProps } from '../Icon'

export function phoneOff02Icon({ iconName = "phone-off02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}