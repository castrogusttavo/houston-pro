import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-outgoing02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-outgoing02icon" />
);