import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorAddSelection01Icon({ iconName = "cursor-add-selection01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}