import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BoxingGlove01Icon({ iconName = "boxing-glove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}