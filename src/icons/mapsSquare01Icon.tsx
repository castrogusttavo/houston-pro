import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapsSquare01Icon({ iconName = "maps-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}