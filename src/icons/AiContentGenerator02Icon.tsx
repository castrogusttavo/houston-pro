import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiContentGenerator02Icon({
  iconName = 'ai-content-generator-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
