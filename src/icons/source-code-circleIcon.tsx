import React from 'react';
import { Icon, IconProps } from '../Icon';

export const source-code-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="source-code-circleicon" />
);