import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-circle04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-circle04icon" />
);