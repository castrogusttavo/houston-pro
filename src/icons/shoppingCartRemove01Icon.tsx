import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingCartRemove01Icon({ iconName = "shopping-cart-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}