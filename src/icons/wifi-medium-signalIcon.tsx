import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wifi-medium-signalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wifi-medium-signalicon" />
);