import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loginCircle02Icon({ iconName = "login-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}