import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowLeft01Icon({ iconName = "circle-arrow-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}