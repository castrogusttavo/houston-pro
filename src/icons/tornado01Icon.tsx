import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tornado01Icon({ iconName = "tornado01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}