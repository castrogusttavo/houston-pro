import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarLove02Icon({
  iconName = 'calendar-love02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
