import React from 'react';
import { Icon, IconProps } from '../Icon';

export const user-arrow-left-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="user-arrow-left-righticon" />
);