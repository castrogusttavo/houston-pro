import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowUp02Icon({ iconName = "square-arrow-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}