import React from 'react'
import { Icon, IconProps } from '../Icon'

export function systemUpdate02Icon({ iconName = "system-update02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}