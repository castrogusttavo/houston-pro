import React from 'react'
import { Icon, IconProps } from '../Icon'

export function musicNoteSquare01Icon({ iconName = "music-note-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}