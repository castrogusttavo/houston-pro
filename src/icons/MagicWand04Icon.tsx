import React from 'react';
import { Icon, IconProps } from '../Icon';

export function MagicWand04Icon({
  iconName = 'magic-wand-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
