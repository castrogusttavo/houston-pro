import React from 'react';
import { Icon, IconProps } from '../Icon';

export function LiveStreaming02Icon({
  iconName = 'live-streaming-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
