import React from 'react';
import { Icon, IconProps } from '../Icon';

export const usb-not-connected01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="usb-not-connected01icon" />
);