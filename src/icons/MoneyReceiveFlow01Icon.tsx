import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyReceiveFlow01Icon({
  iconName = 'money-receive-flow01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
