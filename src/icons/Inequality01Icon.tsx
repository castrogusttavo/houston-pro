import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Inequality01Icon({ iconName = "inequality01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}