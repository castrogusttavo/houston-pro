import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-diagonal02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-diagonal02icon" />
);