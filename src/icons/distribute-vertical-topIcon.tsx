import React from 'react';
import { Icon, IconProps } from '../Icon';

export const distribute-vertical-topIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="distribute-vertical-topicon" />
);