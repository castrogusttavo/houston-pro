import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-verticalicon" />
);