import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowLeft02Icon({ iconName = "square-arrow-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}