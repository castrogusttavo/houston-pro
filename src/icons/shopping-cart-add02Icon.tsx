import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-add02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-add02icon" />
);