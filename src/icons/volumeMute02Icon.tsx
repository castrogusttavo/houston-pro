import React from 'react'
import { Icon, IconProps } from '../Icon'

export function volumeMute02Icon({ iconName = "volume-mute02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}