import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckmarkCircle04Icon({
  iconName = 'checkmark-circle04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
