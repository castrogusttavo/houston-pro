import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loginSquare02Icon({ iconName = "login-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}