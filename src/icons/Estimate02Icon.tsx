import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Estimate02Icon({
  iconName = 'estimate02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
