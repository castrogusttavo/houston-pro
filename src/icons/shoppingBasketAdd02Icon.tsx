import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketAdd02Icon({ iconName = "shopping-basket-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}