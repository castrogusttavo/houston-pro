import React from 'react'
import { Icon, IconProps } from '../Icon'

export function OnlineLearning02Icon({ iconName = "online-learning02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}