import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft01Icon({ iconName = "rotate-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}