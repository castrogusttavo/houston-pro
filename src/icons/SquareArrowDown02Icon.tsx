import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDown02Icon({
  iconName = 'square-arrow-down02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
