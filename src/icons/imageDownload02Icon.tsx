import React from 'react'
import { Icon, IconProps } from '../Icon'

export function imageDownload02Icon({ iconName = "image-download02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}