import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserShield02Icon({ iconName = "user-shield02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}