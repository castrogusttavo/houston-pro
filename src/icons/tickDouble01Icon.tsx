import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tickDouble01Icon({ iconName = "tick-double01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}