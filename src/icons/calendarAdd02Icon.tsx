import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarAdd02Icon({ iconName = "calendar-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}