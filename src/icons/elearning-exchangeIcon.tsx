import React from 'react';
import { Icon, IconProps } from '../Icon';

export const elearning-exchangeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="elearning-exchangeicon" />
);