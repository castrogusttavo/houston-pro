import React from 'react'
import { Icon, IconProps } from '../Icon'

export function solarPanel02Icon({ iconName = "solar-panel02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}