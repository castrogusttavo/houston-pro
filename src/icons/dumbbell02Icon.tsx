import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dumbbell02Icon({ iconName = "dumbbell02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}