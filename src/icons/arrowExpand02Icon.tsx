import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowExpand02Icon({ iconName = "arrow-expand02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}