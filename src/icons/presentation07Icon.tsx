import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentation07Icon({ iconName = "presentation07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}