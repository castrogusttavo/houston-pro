import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-offline01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-offline01icon" />
);