import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location10Icon({ iconName = "location10", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}