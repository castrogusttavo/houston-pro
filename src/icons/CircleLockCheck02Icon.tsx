import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockCheck02Icon({
  iconName = 'circle-lock-check02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
