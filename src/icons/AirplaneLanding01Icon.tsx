import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AirplaneLanding01Icon({ iconName = "airplane-landing01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}