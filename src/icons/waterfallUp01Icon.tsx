import React from 'react'
import { Icon, IconProps } from '../Icon'

export function waterfallUp01Icon({ iconName = "waterfall-up01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}