import React from 'react';
import { Icon, IconProps } from '../Icon';

export const plus-minus-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="plus-minus-square01icon" />
);