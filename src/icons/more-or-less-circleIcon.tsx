import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-or-less-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-or-less-circleicon" />
);