import React from 'react'
import { Icon, IconProps } from '../Icon'

export function checkmarkSquare04Icon({ iconName = "checkmark-square04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}