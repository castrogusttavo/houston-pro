import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Infinity02Icon({ iconName = "infinity02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}