import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationOffline03Icon({ iconName = "location-offline03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}