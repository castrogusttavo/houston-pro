import React from 'react'
import { Icon, IconProps } from '../Icon'

export function threeFinger05Icon({ iconName = "three-finger05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}