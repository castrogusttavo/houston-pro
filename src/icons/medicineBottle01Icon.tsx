import React from 'react'
import { Icon, IconProps } from '../Icon'

export function medicineBottle01Icon({ iconName = "medicine-bottle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}