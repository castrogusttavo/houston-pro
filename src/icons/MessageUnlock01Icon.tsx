import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageUnlock01Icon({ iconName = "message-unlock01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}