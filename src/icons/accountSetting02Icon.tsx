import React from 'react'
import { Icon, IconProps } from '../Icon'

export function accountSetting02Icon({ iconName = "account-setting02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}