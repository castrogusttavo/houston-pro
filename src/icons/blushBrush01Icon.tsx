import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blushBrush01Icon({ iconName = "blush-brush01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}