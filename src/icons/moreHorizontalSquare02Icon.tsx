import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreHorizontalSquare02Icon({ iconName = "more-horizontal-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}