import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-square01icon" />
);