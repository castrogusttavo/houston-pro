import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudLittleRain01Icon({ iconName = "sun-cloud-little-rain01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}