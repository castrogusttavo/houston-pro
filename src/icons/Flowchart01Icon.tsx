import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Flowchart01Icon({ iconName = "flowchart01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}