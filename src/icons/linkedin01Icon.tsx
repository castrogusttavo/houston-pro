import React from 'react'
import { Icon, IconProps } from '../Icon'

export function linkedin01Icon({ iconName = "linkedin01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}