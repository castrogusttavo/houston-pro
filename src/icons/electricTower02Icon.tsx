import React from 'react'
import { Icon, IconProps } from '../Icon'

export function electricTower02Icon({ iconName = "electric-tower02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}