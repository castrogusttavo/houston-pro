import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WifiDisconnected04Icon({ iconName = "wifi-disconnected04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}