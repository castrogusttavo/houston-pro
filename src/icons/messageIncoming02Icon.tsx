import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageIncoming02Icon({ iconName = "message-incoming02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}