import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageUpload01Icon({ iconName = "message-upload01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}