import React from 'react'
import { Icon, IconProps } from '../Icon'

export function telescope01Icon({ iconName = "telescope01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}