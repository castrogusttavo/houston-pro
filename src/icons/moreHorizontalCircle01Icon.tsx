import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreHorizontalCircle01Icon({ iconName = "more-horizontal-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}