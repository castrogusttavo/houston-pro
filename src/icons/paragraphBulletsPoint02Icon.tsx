import React from 'react'
import { Icon, IconProps } from '../Icon'

export function paragraphBulletsPoint02Icon({ iconName = "paragraph-bullets-point02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}