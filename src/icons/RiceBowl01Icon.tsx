import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RiceBowl01Icon({ iconName = "rice-bowl01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}