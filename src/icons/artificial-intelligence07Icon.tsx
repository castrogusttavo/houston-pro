import React from 'react';
import { Icon, IconProps } from '../Icon';

export const artificial-intelligence07Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="artificial-intelligence07icon" />
);