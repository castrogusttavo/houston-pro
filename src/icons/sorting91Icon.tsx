import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sorting91Icon({ iconName = "sorting91", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}