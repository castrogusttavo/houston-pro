import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cloudAngledRainZapIcon({ iconName = "cloud-angled-rain-zap", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}