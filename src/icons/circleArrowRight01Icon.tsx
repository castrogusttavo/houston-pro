import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowRight01Icon({ iconName = "circle-arrow-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}