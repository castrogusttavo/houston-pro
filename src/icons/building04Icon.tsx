import React from 'react'
import { Icon, IconProps } from '../Icon'

export function building04Icon({ iconName = "building04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}