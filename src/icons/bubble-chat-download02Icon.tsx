import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-download02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-download02icon" />
);