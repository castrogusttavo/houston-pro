import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorProgress02Icon({
  iconName = 'cursor-progress02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
