import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudLittleRain02Icon({ iconName = "sun-cloud-little-rain02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}