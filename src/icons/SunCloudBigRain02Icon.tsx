import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudBigRain02Icon({ iconName = "sun-cloud-big-rain02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}