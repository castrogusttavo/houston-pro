import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserSettings02Icon({
  iconName = 'user-settings02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
