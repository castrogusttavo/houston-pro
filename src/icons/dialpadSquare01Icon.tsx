import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dialpadSquare01Icon({ iconName = "dialpad-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}