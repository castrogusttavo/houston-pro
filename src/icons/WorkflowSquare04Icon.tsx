import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare04Icon({ iconName = "workflow-square04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}