import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Navigator02Icon({ iconName = "navigator02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}