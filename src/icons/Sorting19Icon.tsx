import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting19Icon({ iconName = "sorting19", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}