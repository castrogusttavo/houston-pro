import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-add-selection02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-add-selection02icon" />
);