import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dollarReceive02Icon({ iconName = "dollar-receive02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}