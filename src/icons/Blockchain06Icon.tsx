import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Blockchain06Icon({ iconName = "blockchain06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}