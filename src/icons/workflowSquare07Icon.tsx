import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare07Icon({ iconName = "workflow-square07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}