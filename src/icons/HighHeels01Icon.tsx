import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HighHeels01Icon({
  iconName = 'high-heels01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
