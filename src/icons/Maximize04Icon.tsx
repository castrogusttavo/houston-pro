import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Maximize04Icon({
  iconName = 'maximize04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
