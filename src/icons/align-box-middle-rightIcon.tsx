import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-box-middle-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-box-middle-righticon" />
);