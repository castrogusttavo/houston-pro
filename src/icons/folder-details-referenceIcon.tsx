import React from 'react';
import { Icon, IconProps } from '../Icon';

export const folder-details-referenceIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="folder-details-referenceicon" />
);