import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chartBubble01Icon({ iconName = "chart-bubble01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}