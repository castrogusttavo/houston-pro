import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragRight01Icon({ iconName = "drag-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}