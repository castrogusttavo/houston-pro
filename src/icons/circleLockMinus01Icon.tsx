import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleLockMinus01Icon({ iconName = "circle-lock-minus01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}