import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragLeft04Icon({ iconName = "drag-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}