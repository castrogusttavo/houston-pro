import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-move-left-upIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-move-left-upicon" />
);