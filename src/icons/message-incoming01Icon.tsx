import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-incoming01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-incoming01icon" />
);