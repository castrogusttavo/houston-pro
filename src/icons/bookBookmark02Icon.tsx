import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookBookmark02Icon({ iconName = "book-bookmark02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}