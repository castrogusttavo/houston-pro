import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Scooter03Icon({ iconName = 'scooter03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
