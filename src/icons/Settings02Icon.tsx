import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Settings02Icon({ iconName = "settings02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}