import React from 'react';
import { Icon, IconProps } from '../Icon';

export const package-out-of-stockIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="package-out-of-stockicon" />
);