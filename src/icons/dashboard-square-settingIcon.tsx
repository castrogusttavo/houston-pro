import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-square-settingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-square-settingicon" />
);