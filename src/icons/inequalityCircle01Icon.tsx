import React from 'react'
import { Icon, IconProps } from '../Icon'

export function inequalityCircle01Icon({ iconName = "inequality-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}