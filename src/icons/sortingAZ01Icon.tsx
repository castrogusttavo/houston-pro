import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sortingAZ01Icon({ iconName = "sorting-a-z01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}