import React from 'react'
import { Icon, IconProps } from '../Icon'

export function smartWatch02Icon({ iconName = "smart-watch02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}