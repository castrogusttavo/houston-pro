import React from 'react'
import { Icon, IconProps } from '../Icon'

export function musicNote02Icon({ iconName = "music-note02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}