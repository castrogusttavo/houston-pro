import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageNotFound02Icon({ iconName = "image-not-found02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}