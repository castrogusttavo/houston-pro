import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorInfo02Icon({ iconName = "cursor-info02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}