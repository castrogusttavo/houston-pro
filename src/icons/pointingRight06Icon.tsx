import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingRight06Icon({ iconName = "pointing-right06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}