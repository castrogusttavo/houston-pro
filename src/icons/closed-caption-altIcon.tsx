import React from 'react';
import { Icon, IconProps } from '../Icon';

export const closed-caption-altIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="closed-caption-alticon" />
);