import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketCheckIn03Icon({ iconName = "shopping-basket-check-in03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}