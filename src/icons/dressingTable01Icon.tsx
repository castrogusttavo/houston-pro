import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dressingTable01Icon({ iconName = "dressing-table01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}