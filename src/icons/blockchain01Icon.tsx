import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blockchain01Icon({ iconName = "blockchain01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}