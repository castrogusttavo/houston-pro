import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InstallingUpdates02Icon({ iconName = "installing-updates02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}