import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-lock-minus02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-lock-minus02icon" />
);