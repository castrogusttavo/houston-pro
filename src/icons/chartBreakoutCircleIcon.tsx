import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chartBreakoutCircleIcon({ iconName = "chart-breakout-circle", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}