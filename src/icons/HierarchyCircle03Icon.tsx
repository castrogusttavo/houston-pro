import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchyCircle03Icon({ iconName = "hierarchy-circle03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}