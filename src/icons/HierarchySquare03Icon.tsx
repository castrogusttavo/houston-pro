import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare03Icon({ iconName = "hierarchy-square03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}