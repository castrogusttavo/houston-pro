import React from 'react'
import { Icon, IconProps } from '../Icon'

export function onlineLearning01Icon({ iconName = "online-learning01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}