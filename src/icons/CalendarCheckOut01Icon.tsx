import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarCheckOut01Icon({ iconName = "calendar-check-out01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}