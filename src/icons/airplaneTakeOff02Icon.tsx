import React from 'react'
import { Icon, IconProps } from '../Icon'

export function airplaneTakeOff02Icon({ iconName = "airplane-take-off02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}