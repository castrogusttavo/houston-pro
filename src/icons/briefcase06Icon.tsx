import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase06Icon({ iconName = "briefcase06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}