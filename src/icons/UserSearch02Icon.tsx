import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserSearch02Icon({
  iconName = 'user-search02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
