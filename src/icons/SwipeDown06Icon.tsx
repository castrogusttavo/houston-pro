import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown06Icon({
  iconName = 'swipe-down06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
