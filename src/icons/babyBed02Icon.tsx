import React from 'react'
import { Icon, IconProps } from '../Icon'

export function babyBed02Icon({ iconName = "baby-bed02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}