import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userLove01Icon({ iconName = "user-love01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}