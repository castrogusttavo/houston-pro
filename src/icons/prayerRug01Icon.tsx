import React from 'react'
import { Icon, IconProps } from '../Icon'

export function prayerRug01Icon({ iconName = "prayer-rug01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}