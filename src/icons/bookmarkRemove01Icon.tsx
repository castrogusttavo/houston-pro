import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookmarkRemove01Icon({ iconName = "bookmark-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}