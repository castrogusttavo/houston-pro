import React from 'react';
import { Icon, IconProps } from '../Icon';

export const inequality-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="inequality-circle01icon" />
);