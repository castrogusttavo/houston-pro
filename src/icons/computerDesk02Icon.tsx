import React from 'react'
import { Icon, IconProps } from '../Icon'

export function computerDesk02Icon({ iconName = "computer-desk02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}