import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraRotated02Icon({ iconName = "camera-rotated02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}