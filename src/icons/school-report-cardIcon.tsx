import React from 'react';
import { Icon, IconProps } from '../Icon';

export const school-report-cardIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="school-report-cardicon" />
);