import React from 'react'
import { Icon, IconProps } from '../Icon'

export function customerService02Icon({ iconName = "customer-service02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}