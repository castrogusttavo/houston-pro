import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rootFirstBracketIcon({ iconName = "root-first-bracket", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}