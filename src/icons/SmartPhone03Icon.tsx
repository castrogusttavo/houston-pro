import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SmartPhone03Icon({ iconName = "smart-phone03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}