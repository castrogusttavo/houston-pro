import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowDataTransferHorizontalIcon({ iconName = "square-arrow-data-transfer-horizontal", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}