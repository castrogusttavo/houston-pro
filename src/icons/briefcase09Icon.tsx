import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase09Icon({ iconName = "briefcase09", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}