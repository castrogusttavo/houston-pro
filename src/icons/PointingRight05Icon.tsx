import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight05Icon({ iconName = "pointing-right05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}