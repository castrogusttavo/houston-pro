import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sortingAZ02Icon({ iconName = "sorting-a-z02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}