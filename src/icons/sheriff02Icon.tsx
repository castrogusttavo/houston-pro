import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sheriff02Icon({ iconName = "sheriff02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}