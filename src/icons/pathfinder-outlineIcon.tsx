import React from 'react';
import { Icon, IconProps } from '../Icon';

export const pathfinder-outlineIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="pathfinder-outlineicon" />
);