import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-angled-rain-zap01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-angled-rain-zap01icon" />
);