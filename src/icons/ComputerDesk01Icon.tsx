import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ComputerDesk01Icon({
  iconName = 'computer-desk01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
