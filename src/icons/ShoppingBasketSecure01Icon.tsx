import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketSecure01Icon({ iconName = "shopping-basket-secure01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}