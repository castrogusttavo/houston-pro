import React from 'react'
import { Icon, IconProps } from '../Icon'

export function walletDone02Icon({ iconName = "wallet-done02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}