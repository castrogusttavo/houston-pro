import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailSetting02Icon({ iconName = "mail-setting02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}