import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dental-broken-toothIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dental-broken-toothicon" />
);