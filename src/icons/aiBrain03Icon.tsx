import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiBrain03Icon({ iconName = "ai-brain03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}