import React from 'react'
import { Icon, IconProps } from '../Icon'

export function backward01Icon({ iconName = "backward01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}