import React from 'react';
import { Icon, IconProps } from '../Icon';

export const credit-card-not-acceptIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="credit-card-not-accepticon" />
);