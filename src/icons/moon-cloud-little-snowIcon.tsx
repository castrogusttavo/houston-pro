import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-little-snowIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-little-snowicon" />
);