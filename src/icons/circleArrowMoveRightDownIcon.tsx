import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowMoveRightDownIcon({ iconName = "circle-arrow-move-right-down", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}