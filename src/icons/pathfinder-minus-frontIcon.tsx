import React from 'react';
import { Icon, IconProps } from '../Icon';

export const pathfinder-minus-frontIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="pathfinder-minus-fronticon" />
);