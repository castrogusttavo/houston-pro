import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-mid-rain02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-mid-rain02icon" />
);