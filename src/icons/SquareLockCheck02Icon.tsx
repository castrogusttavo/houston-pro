import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockCheck02Icon({
  iconName = 'square-lock-check02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
