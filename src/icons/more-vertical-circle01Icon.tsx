import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-vertical-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-vertical-circle01icon" />
);