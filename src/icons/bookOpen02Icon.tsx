import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookOpen02Icon({ iconName = "book-open02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}