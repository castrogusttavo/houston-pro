import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarSetting02Icon({
  iconName = 'calendar-setting02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
