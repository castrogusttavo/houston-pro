import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-check-in01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-check-in01icon" />
);