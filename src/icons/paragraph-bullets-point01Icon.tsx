import React from 'react';
import { Icon, IconProps } from '../Icon';

export const paragraph-bullets-point01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="paragraph-bullets-point01icon" />
);