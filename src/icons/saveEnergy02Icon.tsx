import React from 'react'
import { Icon, IconProps } from '../Icon'

export function saveEnergy02Icon({ iconName = "save-energy02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}