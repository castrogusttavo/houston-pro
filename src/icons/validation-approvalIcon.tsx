import React from 'react';
import { Icon, IconProps } from '../Icon';

export const validation-approvalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="validation-approvalicon" />
);