import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cabinet04Icon({ iconName = "cabinet04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}