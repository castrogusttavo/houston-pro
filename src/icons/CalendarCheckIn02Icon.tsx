import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarCheckIn02Icon({ iconName = "calendar-check-in02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}