import React from 'react';
import { Icon, IconProps } from '../Icon';

export const notification-block01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="notification-block01icon" />
);