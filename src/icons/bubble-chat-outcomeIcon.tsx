import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-outcomeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-outcomeicon" />
);