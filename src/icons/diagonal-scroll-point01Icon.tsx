import React from 'react';
import { Icon, IconProps } from '../Icon';

export const diagonal-scroll-point01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="diagonal-scroll-point01icon" />
);