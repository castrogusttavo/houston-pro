import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingRight02Icon({ iconName = "hand-pointing-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}