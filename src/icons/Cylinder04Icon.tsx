import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cylinder04Icon({
  iconName = 'cylinder04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
