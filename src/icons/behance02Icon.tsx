import React from 'react'
import { Icon, IconProps } from '../Icon'

export function behance02Icon({ iconName = "behance02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}