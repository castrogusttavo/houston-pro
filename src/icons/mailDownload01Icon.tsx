import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailDownload01Icon({ iconName = "mail-download01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}