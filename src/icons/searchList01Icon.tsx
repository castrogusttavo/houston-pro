import React from 'react'
import { Icon, IconProps } from '../Icon'

export function searchList01Icon({ iconName = "search-list01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}