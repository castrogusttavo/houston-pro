import React from 'react'
import { Icon, IconProps } from '../Icon'

export function smartWatch04Icon({ iconName = "smart-watch04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}