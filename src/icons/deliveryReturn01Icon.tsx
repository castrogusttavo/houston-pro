import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryReturn01Icon({ iconName = "delivery-return01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}