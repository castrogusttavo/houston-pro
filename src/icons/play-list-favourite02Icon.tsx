import React from 'react';
import { Icon, IconProps } from '../Icon';

export const play-list-favourite02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="play-list-favourite02icon" />
);