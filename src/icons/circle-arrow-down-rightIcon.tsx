import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-down-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-down-righticon" />
);