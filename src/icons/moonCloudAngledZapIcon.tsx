import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudAngledZapIcon({ iconName = "moon-cloud-angled-zap", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}