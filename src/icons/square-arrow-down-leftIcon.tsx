import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-down-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-down-lefticon" />
);