import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pentagon01Icon({ iconName = "pentagon01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}