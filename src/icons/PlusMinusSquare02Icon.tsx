import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlusMinusSquare02Icon({
  iconName = 'plus-minus-square02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
