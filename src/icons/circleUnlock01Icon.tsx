import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleUnlock01Icon({ iconName = "circle-unlock01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}