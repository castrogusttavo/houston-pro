import React from 'react';
import { Icon, IconProps } from '../Icon';

export const screen-lock-rotationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="screen-lock-rotationicon" />
);