import React from 'react';
import { Icon, IconProps } from '../Icon';

export const keyframes-double-addIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="keyframes-double-addicon" />
);