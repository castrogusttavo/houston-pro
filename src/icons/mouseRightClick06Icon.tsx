import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mouseRightClick06Icon({ iconName = "mouse-right-click06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}