import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-lock-passwordIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-lock-passwordicon" />
);