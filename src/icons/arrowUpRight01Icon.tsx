import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowUpRight01Icon({ iconName = "arrow-up-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}