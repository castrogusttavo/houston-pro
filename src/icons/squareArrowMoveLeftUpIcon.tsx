import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowMoveLeftUpIcon({ iconName = "square-arrow-move-left-up", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}