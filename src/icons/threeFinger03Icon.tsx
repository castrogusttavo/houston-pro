import React from 'react'
import { Icon, IconProps } from '../Icon'

export function threeFinger03Icon({ iconName = "three-finger03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}