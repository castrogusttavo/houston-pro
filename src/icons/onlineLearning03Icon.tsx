import React from 'react'
import { Icon, IconProps } from '../Icon'

export function onlineLearning03Icon({ iconName = "online-learning03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}