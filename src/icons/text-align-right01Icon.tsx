import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-align-right01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-align-right01icon" />
);