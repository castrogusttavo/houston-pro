import React from 'react';
import { Icon, IconProps } from '../Icon';

export const washington-monumentIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="washington-monumenticon" />
);