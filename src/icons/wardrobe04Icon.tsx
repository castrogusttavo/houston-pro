import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wardrobe04Icon({ iconName = "wardrobe04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}