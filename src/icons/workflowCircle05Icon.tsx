import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowCircle05Icon({ iconName = "workflow-circle05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}