import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blockchain05Icon({ iconName = "blockchain05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}