import React from 'react'
import { Icon, IconProps } from '../Icon'

export function legalDocument01Icon({ iconName = "legal-document01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}