import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiCloud01Icon({
  iconName = 'ai-cloud01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
