import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-secure02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-secure02icon" />
);