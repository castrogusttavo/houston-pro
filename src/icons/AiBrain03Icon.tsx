import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiBrain03Icon({
  iconName = 'ai-brain03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
