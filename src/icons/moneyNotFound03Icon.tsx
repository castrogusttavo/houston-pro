import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyNotFound03Icon({ iconName = "money-not-found03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}