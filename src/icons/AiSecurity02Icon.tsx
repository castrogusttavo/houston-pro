import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiSecurity02Icon({ iconName = "ai-security02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}