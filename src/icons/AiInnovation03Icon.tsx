import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiInnovation03Icon({ iconName = "ai-innovation03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}