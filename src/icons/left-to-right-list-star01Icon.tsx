import React from 'react';
import { Icon, IconProps } from '../Icon';

export const left-to-right-list-star01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="left-to-right-list-star01icon" />
);