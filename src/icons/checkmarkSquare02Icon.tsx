import React from 'react'
import { Icon, IconProps } from '../Icon'

export function checkmarkSquare02Icon({ iconName = "checkmark-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}