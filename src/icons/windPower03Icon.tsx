import React from 'react'
import { Icon, IconProps } from '../Icon'

export function windPower03Icon({ iconName = "wind-power03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}