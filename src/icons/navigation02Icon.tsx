import React from 'react'
import { Icon, IconProps } from '../Icon'

export function navigation02Icon({ iconName = "navigation02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}