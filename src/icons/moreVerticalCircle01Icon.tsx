import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreVerticalCircle01Icon({ iconName = "more-vertical-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}