import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorRectangleSelection01Icon({
  iconName = 'cursor-rectangle-selection01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
