import React from 'react';
import { Icon, IconProps } from '../Icon';

export const plus-minus-circle02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="plus-minus-circle02icon" />
);