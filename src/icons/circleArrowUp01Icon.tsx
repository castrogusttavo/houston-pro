import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowUp01Icon({ iconName = "circle-arrow-up01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}