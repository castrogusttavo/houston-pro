import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudSlowWindIcon({ iconName = "moon-cloud-slow-wind", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}