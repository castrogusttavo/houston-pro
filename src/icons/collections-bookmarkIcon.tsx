import React from 'react';
import { Icon, IconProps } from '../Icon';

export const collections-bookmarkIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="collections-bookmarkicon" />
);