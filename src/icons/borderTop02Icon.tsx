import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderTop02Icon({ iconName = "border-top02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}