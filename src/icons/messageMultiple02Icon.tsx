import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageMultiple02Icon({ iconName = "message-multiple02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}