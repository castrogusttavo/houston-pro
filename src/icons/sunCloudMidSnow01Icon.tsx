import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudMidSnow01Icon({ iconName = "sun-cloud-mid-snow01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}