import React from 'react';
import { Icon, IconProps } from '../Icon';

export const battery-charging01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="battery-charging01icon" />
);