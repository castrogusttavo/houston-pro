import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Redo02Icon({
  iconName = 'redo-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
