import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-expand01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-expand01icon" />
);