import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudAngledZap01Icon({ iconName = "sun-cloud-angled-zap01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}