import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingRight01Icon({ iconName = "pointing-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}