import React from 'react';
import { Icon, IconProps } from '../Icon';

export const camera-night-mode02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="camera-night-mode02icon" />
);