import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDown02Icon({ iconName = "arrow-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}