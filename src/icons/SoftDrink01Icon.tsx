import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SoftDrink01Icon({
  iconName = 'soft-drink01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
