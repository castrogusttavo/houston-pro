import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location04Icon({ iconName = "location04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}