import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketCheckIn02Icon({ iconName = "shopping-basket-check-in02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}