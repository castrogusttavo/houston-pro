import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-align-justify-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-align-justify-lefticon" />
);