import React from 'react'
import { Icon, IconProps } from '../Icon'

export function backpack02Icon({ iconName = "backpack02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}