import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mouse-left-click06Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mouse-left-click06icon" />
);