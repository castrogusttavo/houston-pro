import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateRight04Icon({ iconName = "rotate-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}