import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarCheckIn01Icon({ iconName = "calendar-check-in01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}