import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderLeft01Icon({ iconName = "border-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}