import React from 'react';
import { Icon, IconProps } from '../Icon';

export function CircleArrowUpRight02Icon({
  iconName = 'circle-arrow-up-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
