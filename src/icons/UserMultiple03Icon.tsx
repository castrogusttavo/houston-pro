import React from 'react';
import { Icon, IconProps } from '../Icon';

export function UserMultiple03Icon({
  iconName = 'user-multiple-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
