import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiCloud02Icon({ iconName = "ai-cloud02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}