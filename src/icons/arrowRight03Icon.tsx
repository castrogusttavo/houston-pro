import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowRight03Icon({ iconName = "arrow-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}