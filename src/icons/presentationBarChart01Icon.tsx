import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentationBarChart01Icon({ iconName = "presentation-bar-chart01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}