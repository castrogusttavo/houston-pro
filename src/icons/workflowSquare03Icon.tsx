import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare03Icon({ iconName = "workflow-square03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}