import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraAdd01Icon({ iconName = "camera-add01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}