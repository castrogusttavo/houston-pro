import React from 'react'
import { Icon, IconProps } from '../Icon'

export function structure04Icon({ iconName = "structure04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}