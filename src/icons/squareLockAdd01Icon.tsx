import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareLockAdd01Icon({ iconName = "square-lock-add01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}