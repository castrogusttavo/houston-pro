import React from 'react'
import { Icon, IconProps } from '../Icon'

export function virtualRealityVr02Icon({ iconName = "virtual-reality-vr02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}