import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SoftDrink02Icon({
  iconName = 'soft-drink02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
