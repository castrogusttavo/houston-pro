import React from 'react'
import { Icon, IconProps } from '../Icon'

export function downloadCircle01Icon({ iconName = "download-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}