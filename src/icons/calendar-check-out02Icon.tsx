import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-check-out02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-check-out02icon" />
);