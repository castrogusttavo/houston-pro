import React from 'react';
import { Icon, IconProps } from '../Icon';

export const money-receive-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="money-receive-circleicon" />
);