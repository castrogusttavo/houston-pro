import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PaintBrush02Icon({
  iconName = 'paint-brush02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
