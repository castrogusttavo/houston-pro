import React from 'react'
import { Icon, IconProps } from '../Icon'

export function doNotTouch02Icon({ iconName = "do-not-touch02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}