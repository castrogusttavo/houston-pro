import React from 'react';
import { Icon, IconProps } from '../Icon';

export const inequality-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="inequality-square02icon" />
);