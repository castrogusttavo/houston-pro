import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchpad04Icon({ iconName = "touchpad04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}