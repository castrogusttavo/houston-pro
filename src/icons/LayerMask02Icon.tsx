import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LayerMask02Icon({
  iconName = 'layer-mask02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
