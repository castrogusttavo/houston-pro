import React from 'react'
import { Icon, IconProps } from '../Icon'

export function punchingBall01Icon({ iconName = "punching-ball01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}