import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowMoveDownRightIcon({ iconName = "circle-arrow-move-down-right", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}