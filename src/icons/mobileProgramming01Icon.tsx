import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mobileProgramming01Icon({ iconName = "mobile-programming01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}