import React from 'react'
import { Icon, IconProps } from '../Icon'

export function backpack03Icon({ iconName = "backpack03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}