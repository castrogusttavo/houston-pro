import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketCheckOut01Icon({ iconName = "shopping-basket-check-out01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}