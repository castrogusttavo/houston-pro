import React from 'react';
import { Icon, IconProps } from '../Icon';

export const right-to-left-block-quoteIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="right-to-left-block-quoteicon" />
);