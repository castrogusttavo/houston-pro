import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-firstline-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-firstline-righticon" />
);