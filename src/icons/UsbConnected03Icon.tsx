import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UsbConnected03Icon({
  iconName = 'usb-connected03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
