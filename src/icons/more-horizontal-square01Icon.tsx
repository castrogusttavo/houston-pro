import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-horizontal-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-horizontal-square01icon" />
);