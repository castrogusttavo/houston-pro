import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-data-transfer-diagonalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-data-transfer-diagonalicon" />
);