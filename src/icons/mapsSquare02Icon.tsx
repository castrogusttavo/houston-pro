import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapsSquare02Icon({ iconName = "maps-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}