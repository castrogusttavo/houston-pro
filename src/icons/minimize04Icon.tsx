import React from 'react'
import { Icon, IconProps } from '../Icon'

export function minimize04Icon({ iconName = "minimize04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}