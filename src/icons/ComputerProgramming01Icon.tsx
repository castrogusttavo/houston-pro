import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ComputerProgramming01Icon({ iconName = "computer-programming01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}