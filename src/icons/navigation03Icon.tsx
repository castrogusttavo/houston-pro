import React from 'react'
import { Icon, IconProps } from '../Icon'

export function navigation03Icon({ iconName = "navigation03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}