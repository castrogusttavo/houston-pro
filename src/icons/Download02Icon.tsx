import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Download02Icon({ iconName = "download02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}