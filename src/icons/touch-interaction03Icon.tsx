import React from 'react';
import { Icon, IconProps } from '../Icon';

export const touch-interaction03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="touch-interaction03icon" />
);