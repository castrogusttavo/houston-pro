import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowReload02Icon({ iconName = "square-arrow-reload02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}