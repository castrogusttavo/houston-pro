import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketFavorite03Icon({ iconName = "shopping-basket-favorite03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}