import React from 'react';
import { Icon, IconProps } from '../Icon';

export const thumbs-down-ellipseIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="thumbs-down-ellipseicon" />
);