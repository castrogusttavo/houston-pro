import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AudioBook04Icon({ iconName = "audio-book04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}