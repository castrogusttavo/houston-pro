import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight03Icon({
  iconName = 'pointing-right03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
