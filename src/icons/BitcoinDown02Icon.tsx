import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BitcoinDown02Icon({ iconName = "bitcoin-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}