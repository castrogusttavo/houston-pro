import React from 'react'
import { Icon, IconProps } from '../Icon'

export function simcard02Icon({ iconName = "simcard02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}