import React from 'react'
import { Icon, IconProps } from '../Icon'

export function airplane02Icon({ iconName = "airplane02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}