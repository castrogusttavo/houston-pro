import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-centerline-center-topIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-centerline-center-topicon" />
);