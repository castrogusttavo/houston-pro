import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare10Icon({ iconName = "workflow-square10", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}