import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-right-doubleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-right-doubleicon" />
);