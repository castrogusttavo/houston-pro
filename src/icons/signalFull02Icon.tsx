import React from 'react'
import { Icon, IconProps } from '../Icon'

export function signalFull02Icon({ iconName = "signal-full02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}