import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PieChart08Icon({
  iconName = 'pie-chart-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
