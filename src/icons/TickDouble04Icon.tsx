import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TickDouble04Icon({
  iconName = 'tick-double04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
