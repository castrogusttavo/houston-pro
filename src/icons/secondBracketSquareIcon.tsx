import React from 'react'
import { Icon, IconProps } from '../Icon'

export function secondBracketSquareIcon({ iconName = "second-bracket-square", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}