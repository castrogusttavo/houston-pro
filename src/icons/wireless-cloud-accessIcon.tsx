import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wireless-cloud-accessIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wireless-cloud-accessicon" />
);