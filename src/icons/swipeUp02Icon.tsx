import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeUp02Icon({ iconName = "swipe-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}