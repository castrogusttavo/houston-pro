import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiagonalScrollPoint02Icon({
  iconName = 'diagonal-scroll-point02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
