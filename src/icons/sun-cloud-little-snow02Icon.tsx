import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-little-snow02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-little-snow02icon" />
);