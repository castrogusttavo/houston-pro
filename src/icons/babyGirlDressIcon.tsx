import React from 'react'
import { Icon, IconProps } from '../Icon'

export function babyGirlDressIcon({ iconName = "baby-girl-dress", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}