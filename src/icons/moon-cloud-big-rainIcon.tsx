import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-big-rainIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-big-rainicon" />
);