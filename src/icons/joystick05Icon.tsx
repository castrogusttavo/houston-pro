import React from 'react'
import { Icon, IconProps } from '../Icon'

export function joystick05Icon({ iconName = "joystick05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}