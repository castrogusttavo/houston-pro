import React from 'react';
import { Icon, IconProps } from '../Icon';

export const chart-breakout-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="chart-breakout-circleicon" />
);