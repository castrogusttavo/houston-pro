import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting04Icon({ iconName = "sorting04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}