import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeRight04Icon({ iconName = "swipe-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}