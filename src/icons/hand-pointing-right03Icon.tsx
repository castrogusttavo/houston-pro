import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-right03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-right03icon" />
);