import React from 'react'
import { Icon, IconProps } from '../Icon'

export function secondBrecketSquareIcon({ iconName = "second-brecket-square", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}