import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarDownload01Icon({ iconName = "calendar-download01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}