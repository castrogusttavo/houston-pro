import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-favourite02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-favourite02icon" />
);