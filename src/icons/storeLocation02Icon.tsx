import React from 'react'
import { Icon, IconProps } from '../Icon'

export function storeLocation02Icon({ iconName = "store-location02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}