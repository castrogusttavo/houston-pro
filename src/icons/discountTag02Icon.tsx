import React from 'react'
import { Icon, IconProps } from '../Icon'

export function discountTag02Icon({ iconName = "discount-tag02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}