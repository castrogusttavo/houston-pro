import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowLeft01Icon({ iconName = "square-arrow-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}