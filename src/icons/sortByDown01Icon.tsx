import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sortByDown01Icon({ iconName = "sort-by-down01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}