import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp07Icon({ iconName = "swipe-up07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}