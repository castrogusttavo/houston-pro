import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tableLamp02Icon({ iconName = "table-lamp02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}