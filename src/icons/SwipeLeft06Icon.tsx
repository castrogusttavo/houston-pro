import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft06Icon({ iconName = "swipe-left06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}