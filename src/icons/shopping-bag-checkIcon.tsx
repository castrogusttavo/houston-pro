import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-bag-checkIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-bag-checkicon" />
);