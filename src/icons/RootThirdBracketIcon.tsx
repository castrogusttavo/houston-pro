import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RootThirdBracketIcon({
  iconName = 'root-third-bracket',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
