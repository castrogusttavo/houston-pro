import React from 'react'
import { Icon, IconProps } from '../Icon'

export function linkSquare02Icon({ iconName = "link-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}