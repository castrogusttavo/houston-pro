import React from 'react';
import { Icon, IconProps } from '../Icon';

export const music-note-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="music-note-square01icon" />
);