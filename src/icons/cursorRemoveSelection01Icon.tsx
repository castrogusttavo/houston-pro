import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorRemoveSelection01Icon({ iconName = "cursor-remove-selection01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}