import React from 'react'
import { Icon, IconProps } from '../Icon'

export function joystick03Icon({ iconName = "joystick03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}