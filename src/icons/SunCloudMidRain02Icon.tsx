import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudMidRain02Icon({
  iconName = 'sun-cloud-mid-rain02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
