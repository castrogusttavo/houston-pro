import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-move-up-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-move-up-righticon" />
);