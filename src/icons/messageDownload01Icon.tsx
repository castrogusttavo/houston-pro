import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageDownload01Icon({ iconName = "message-download01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}