import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RenewableEnergy01Icon({ iconName = "renewable-energy01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}