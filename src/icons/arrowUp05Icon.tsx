import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowUp05Icon({ iconName = "arrow-up05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}