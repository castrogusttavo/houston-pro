import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowUp02Icon({ iconName = "circle-arrow-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}