import React from 'react'
import { Icon, IconProps } from '../Icon'

export function fourFinger03Icon({ iconName = "four-finger03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}