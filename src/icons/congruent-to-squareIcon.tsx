import React from 'react';
import { Icon, IconProps } from '../Icon';

export const congruent-to-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="congruent-to-squareicon" />
);