import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tornado02Icon({ iconName = "tornado02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}