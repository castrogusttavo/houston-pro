import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailUpload01Icon({ iconName = "mail-upload01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}