import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-secureIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-secureicon" />
);