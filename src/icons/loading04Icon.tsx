import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loading04Icon({ iconName = "loading04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}