import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-horizontal-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-horizontal-square02icon" />
);