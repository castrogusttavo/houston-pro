import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketRemove01Icon({ iconName = "shopping-basket-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}