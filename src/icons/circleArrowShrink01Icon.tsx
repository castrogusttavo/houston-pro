import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowShrink01Icon({ iconName = "circle-arrow-shrink01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}