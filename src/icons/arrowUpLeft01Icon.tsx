import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowUpLeft01Icon({ iconName = "arrow-up-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}