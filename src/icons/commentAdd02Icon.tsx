import React from 'react'
import { Icon, IconProps } from '../Icon'

export function commentAdd02Icon({ iconName = "comment-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}