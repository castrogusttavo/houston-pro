import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-square03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-square03icon" />
);