import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasket02Icon({ iconName = "shopping-basket02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}