import React from 'react';
import { Icon, IconProps } from '../Icon';

export const airplane-take-off01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="airplane-take-off01icon" />
);