import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchyCircle02Icon({ iconName = "hierarchy-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}