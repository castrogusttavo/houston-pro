import React from 'react';
import { Icon, IconProps } from '../Icon';

export const camera-night-mode01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="camera-night-mode01icon" />
);