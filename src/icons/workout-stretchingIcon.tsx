import React from 'react';
import { Icon, IconProps } from '../Icon';

export const workout-stretchingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="workout-stretchingicon" />
);