import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WeightScale01Icon({
  iconName = 'weight-scale01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
