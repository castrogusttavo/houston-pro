import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EarRings01Icon({
  iconName = 'ear-rings01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
