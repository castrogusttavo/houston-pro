import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorRectangleSelection02Icon({ iconName = "cursor-rectangle-selection02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}