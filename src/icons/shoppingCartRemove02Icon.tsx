import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingCartRemove02Icon({ iconName = "shopping-cart-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}