import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-lock-check02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-lock-check02icon" />
);