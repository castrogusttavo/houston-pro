import React from 'react'
import { Icon, IconProps } from '../Icon'

export function invoice03Icon({ iconName = "invoice03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}