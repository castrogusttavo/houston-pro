import React from 'react'
import { Icon, IconProps } from '../Icon'

export function alignBoxBottomRightIcon({ iconName = "align-box-bottom-right", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}