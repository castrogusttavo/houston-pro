import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MinusPlusCircle02Icon({ iconName = "minus-plus-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}