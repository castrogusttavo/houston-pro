import React from 'react'
import { Icon, IconProps } from '../Icon'

export function textAlignLeft01Icon({ iconName = "text-align-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}