import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeDown02Icon({ iconName = "swipe-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}