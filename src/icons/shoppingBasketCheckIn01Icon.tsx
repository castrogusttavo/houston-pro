import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketCheckIn01Icon({ iconName = "shopping-basket-check-in01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}