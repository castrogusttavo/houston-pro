import React from 'react'
import { Icon, IconProps } from '../Icon'

export function imageUpload01Icon({ iconName = "image-upload01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}