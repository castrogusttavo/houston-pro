import React from 'react'
import { Icon, IconProps } from '../Icon'

export function storeVerified01Icon({ iconName = "store-verified01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}