import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BubbleChatDownload01Icon({
  iconName = 'bubble-chat-download01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
