import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wifiDisconnected03Icon({ iconName = "wifi-disconnected03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}