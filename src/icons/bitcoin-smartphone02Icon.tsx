import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bitcoin-smartphone02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bitcoin-smartphone02icon" />
);