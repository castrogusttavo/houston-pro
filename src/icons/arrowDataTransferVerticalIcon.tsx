import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDataTransferVerticalIcon({ iconName = "arrow-data-transfer-vertical", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}