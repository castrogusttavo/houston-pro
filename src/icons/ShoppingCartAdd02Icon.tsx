import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartAdd02Icon({ iconName = "shopping-cart-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}