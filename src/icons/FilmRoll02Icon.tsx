import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FilmRoll02Icon({
  iconName = 'film-roll02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
