import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarFavorite02Icon({
  iconName = 'calendar-favorite02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
