import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-vertical-centerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-vertical-centericon" />
);