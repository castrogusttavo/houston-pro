import React from 'react'
import { Icon, IconProps } from '../Icon'

export function searchList02Icon({ iconName = "search-list02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}