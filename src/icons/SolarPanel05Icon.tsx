import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SolarPanel05Icon({ iconName = "solar-panel05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}