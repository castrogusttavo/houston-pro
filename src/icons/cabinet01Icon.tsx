import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cabinet01Icon({ iconName = "cabinet01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}