import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft03Icon({ iconName = "rotate-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}