import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapPinpoint02Icon({ iconName = "map-pinpoint02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}