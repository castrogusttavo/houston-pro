import React from 'react';
import { Icon, IconProps } from '../Icon';

export const root-third-bracketIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="root-third-bracketicon" />
);