import React from 'react'
import { Icon, IconProps } from '../Icon'

export function walletDone01Icon({ iconName = "wallet-done01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}