import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyNotFound03Icon({
  iconName = 'money-not-found03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
