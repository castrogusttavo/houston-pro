import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-cancelIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-cancelicon" />
);