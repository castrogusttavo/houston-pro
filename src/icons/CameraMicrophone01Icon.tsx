import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraMicrophone01Icon({
  iconName = 'camera-microphone01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
