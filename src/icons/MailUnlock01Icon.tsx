import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailUnlock01Icon({ iconName = "mail-unlock01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}