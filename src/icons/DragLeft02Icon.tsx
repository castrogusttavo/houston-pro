import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragLeft02Icon({
  iconName = 'drag-left02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
