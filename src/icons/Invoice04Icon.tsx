import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Invoice04Icon({ iconName = "invoice04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}