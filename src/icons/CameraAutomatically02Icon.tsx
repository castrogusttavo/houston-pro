import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraAutomatically02Icon({
  iconName = 'camera-automatically02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
