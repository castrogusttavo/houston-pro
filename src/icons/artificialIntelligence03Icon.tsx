import React from 'react'
import { Icon, IconProps } from '../Icon'

export function artificialIntelligence03Icon({ iconName = "artificial-intelligence03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}