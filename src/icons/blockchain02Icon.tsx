import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blockchain02Icon({ iconName = "blockchain02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}