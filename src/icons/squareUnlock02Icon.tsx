import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareUnlock02Icon({ iconName = "square-unlock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}