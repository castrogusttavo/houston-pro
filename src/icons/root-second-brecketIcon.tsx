import React from 'react';
import { Icon, IconProps } from '../Icon';

export const root-second-brecketIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="root-second-brecketicon" />
);