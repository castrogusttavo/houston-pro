import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiBrain02Icon({ iconName = "ai-brain02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}