import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailMinus01Icon({ iconName = "mail-minus01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}