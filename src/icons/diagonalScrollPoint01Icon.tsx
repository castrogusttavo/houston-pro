import React from 'react'
import { Icon, IconProps } from '../Icon'

export function diagonalScrollPoint01Icon({ iconName = "diagonal-scroll-point01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}