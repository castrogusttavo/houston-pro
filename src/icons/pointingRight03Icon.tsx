import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingRight03Icon({ iconName = "pointing-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}