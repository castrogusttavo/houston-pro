import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft05Icon({ iconName = "swipe-left05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}