import React from 'react'
import { Icon, IconProps } from '../Icon'

export function scooter04Icon({ iconName = "scooter04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}