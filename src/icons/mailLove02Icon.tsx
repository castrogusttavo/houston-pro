import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailLove02Icon({ iconName = "mail-love02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}