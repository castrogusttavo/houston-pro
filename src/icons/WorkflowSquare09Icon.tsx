import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare09Icon({ iconName = "workflow-square09", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}