import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowReload01Icon({ iconName = "square-arrow-reload01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}