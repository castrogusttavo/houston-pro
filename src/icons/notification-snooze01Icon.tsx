import React from 'react';
import { Icon, IconProps } from '../Icon';

export const notification-snooze01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="notification-snooze01icon" />
);