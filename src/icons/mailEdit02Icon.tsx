import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailEdit02Icon({ iconName = "mail-edit02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}