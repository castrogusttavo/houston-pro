import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-circle03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-circle03icon" />
);