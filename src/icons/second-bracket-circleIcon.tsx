import React from 'react';
import { Icon, IconProps } from '../Icon';

export const second-bracket-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="second-bracket-circleicon" />
);