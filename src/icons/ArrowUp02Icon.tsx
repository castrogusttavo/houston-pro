import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUp02Icon({
  iconName = 'arrow-up02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
