import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-vertical-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-vertical-square01icon" />
);