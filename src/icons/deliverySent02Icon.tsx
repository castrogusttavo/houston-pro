import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliverySent02Icon({ iconName = "delivery-sent02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}