import React from 'react'
import { Icon, IconProps } from '../Icon'

export function comingSoon01Icon({ iconName = "coming-soon01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}