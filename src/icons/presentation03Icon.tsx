import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentation03Icon({ iconName = "presentation03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}