import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudHailstone01Icon({ iconName = "sun-cloud-hailstone01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}