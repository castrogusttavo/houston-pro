import React from 'react';
import { Icon, IconProps } from '../Icon';

export const money-receive-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="money-receive-squareicon" />
);