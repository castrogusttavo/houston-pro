import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiInnovation01Icon({
  iconName = 'ai-innovation01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
