import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wps-office-rectangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wps-office-rectangleicon" />
);