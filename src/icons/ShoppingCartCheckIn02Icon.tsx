import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartCheckIn02Icon({
  iconName = 'shopping-cart-check-in02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
