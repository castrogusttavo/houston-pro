import React from 'react'
import { Icon, IconProps } from '../Icon'

export function usbMemory02Icon({ iconName = "usb-memory02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}