import React from 'react'
import { Icon, IconProps } from '../Icon'

export function powerSocket01Icon({ iconName = "power-socket01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}