import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PinLocation03Icon({ iconName = "pin-location03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}