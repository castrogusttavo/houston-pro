import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationAdd02Icon({ iconName = "location-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}