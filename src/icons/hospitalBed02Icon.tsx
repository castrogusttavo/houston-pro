import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hospitalBed02Icon({ iconName = "hospital-bed02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}