import React from 'react';
import { Icon, IconProps } from '../Icon';

export const image-composition-ovalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="image-composition-ovalicon" />
);