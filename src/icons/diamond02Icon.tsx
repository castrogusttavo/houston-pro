import React from 'react'
import { Icon, IconProps } from '../Icon'

export function diamond02Icon({ iconName = "diamond02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}