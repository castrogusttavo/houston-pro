import React from 'react'
import { Icon, IconProps } from '../Icon'

export function quillWrite01Icon({ iconName = "quill-write01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}