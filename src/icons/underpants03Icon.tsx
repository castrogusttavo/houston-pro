import React from 'react'
import { Icon, IconProps } from '../Icon'

export function underpants03Icon({ iconName = "underpants03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}