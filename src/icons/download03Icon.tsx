import React from 'react'
import { Icon, IconProps } from '../Icon'

export function download03Icon({ iconName = "download03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}