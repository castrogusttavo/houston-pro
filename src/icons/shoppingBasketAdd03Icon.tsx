import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketAdd03Icon({ iconName = "shopping-basket-add03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}