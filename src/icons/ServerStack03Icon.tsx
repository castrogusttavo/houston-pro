import React from 'react';
import { Icon, IconProps } from '../Icon';

export function ServerStack03Icon({
  iconName = 'server-stack-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
