import React from 'react'
import { Icon, IconProps } from '../Icon'

export function imageAdd02Icon({ iconName = "image-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}