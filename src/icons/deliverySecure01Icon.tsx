import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliverySecure01Icon({ iconName = "delivery-secure01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}