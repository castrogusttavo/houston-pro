import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-expand02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-expand02icon" />
);