import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare08Icon({ iconName = "workflow-square08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}