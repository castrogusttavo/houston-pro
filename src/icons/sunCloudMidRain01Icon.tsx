import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudMidRain01Icon({ iconName = "sun-cloud-mid-rain01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}