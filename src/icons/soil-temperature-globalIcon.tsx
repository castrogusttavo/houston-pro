import React from 'react';
import { Icon, IconProps } from '../Icon';

export const soil-temperature-globalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="soil-temperature-globalicon" />
);