import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location08Icon({ iconName = "location08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}