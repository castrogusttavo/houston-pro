import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase05Icon({ iconName = "briefcase05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}