import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-rectangle-selection02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-rectangle-selection02icon" />
);