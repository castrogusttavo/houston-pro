import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AirplaneLanding02Icon({
  iconName = 'airplane-landing02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
