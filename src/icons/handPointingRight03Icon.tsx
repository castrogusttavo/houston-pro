import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingRight03Icon({ iconName = "hand-pointing-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}