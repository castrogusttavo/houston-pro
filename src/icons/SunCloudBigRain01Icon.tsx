import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudBigRain01Icon({
  iconName = 'sun-cloud-big-rain01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
