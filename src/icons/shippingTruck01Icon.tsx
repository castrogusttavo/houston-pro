import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shippingTruck01Icon({ iconName = "shipping-truck01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}