import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-add01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-add01icon" />
);