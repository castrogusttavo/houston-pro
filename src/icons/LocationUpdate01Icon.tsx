import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationUpdate01Icon({ iconName = "location-update01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}