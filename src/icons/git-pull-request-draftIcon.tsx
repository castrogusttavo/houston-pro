import React from 'react';
import { Icon, IconProps } from '../Icon';

export const git-pull-request-draftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="git-pull-request-drafticon" />
);