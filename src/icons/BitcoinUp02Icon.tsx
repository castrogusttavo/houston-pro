import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BitcoinUp02Icon({ iconName = "bitcoin-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}