import React from 'react'
import { Icon, IconProps } from '../Icon'

export function typescript02Icon({ iconName = "typescript02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}