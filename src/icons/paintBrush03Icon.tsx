import React from 'react'
import { Icon, IconProps } from '../Icon'

export function paintBrush03Icon({ iconName = "paint-brush03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}