import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowLeftDoubleIcon({ iconName = "circle-arrow-left-double", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}