import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wardrobe02Icon({ iconName = "wardrobe02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}