import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-diagonal01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-diagonal01icon" />
);