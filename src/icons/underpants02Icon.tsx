import React from 'react'
import { Icon, IconProps } from '../Icon'

export function underpants02Icon({ iconName = "underpants02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}