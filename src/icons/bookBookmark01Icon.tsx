import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookBookmark01Icon({ iconName = "book-bookmark01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}