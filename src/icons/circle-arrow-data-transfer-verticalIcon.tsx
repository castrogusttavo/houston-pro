import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-data-transfer-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-data-transfer-verticalicon" />
);