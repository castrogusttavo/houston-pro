import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryTracking01Icon({
  iconName = 'delivery-tracking01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
