import React from 'react'
import { Icon, IconProps } from '../Icon'

export function download01Icon({ iconName = "download01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}