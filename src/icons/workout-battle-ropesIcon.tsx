import React from 'react';
import { Icon, IconProps } from '../Icon';

export const workout-battle-ropesIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="workout-battle-ropesicon" />
);