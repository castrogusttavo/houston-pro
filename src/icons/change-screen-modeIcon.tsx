import React from 'react';
import { Icon, IconProps } from '../Icon';

export const change-screen-modeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="change-screen-modeicon" />
);