import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorMagicSelection01Icon({ iconName = "cursor-magic-selection01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}