import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowRight02Icon({ iconName = "square-arrow-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}