import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-reload02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-reload02icon" />
);