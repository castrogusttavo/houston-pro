import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreVerticalSquare01Icon({ iconName = "more-vertical-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}