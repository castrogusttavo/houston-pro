import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-check01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-check01icon" />
);