import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragRight04Icon({ iconName = "drag-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}