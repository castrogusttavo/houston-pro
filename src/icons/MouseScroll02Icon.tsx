import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseScroll02Icon({
  iconName = 'mouse-scroll02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
