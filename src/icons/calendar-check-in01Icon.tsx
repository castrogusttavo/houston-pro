import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-check-in01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-check-in01icon" />
);