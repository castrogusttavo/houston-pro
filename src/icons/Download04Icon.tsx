import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Download04Icon({ iconName = "download04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}