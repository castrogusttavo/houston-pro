import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-square02icon" />
);