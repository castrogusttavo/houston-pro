import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cloudAngledRainIcon({ iconName = "cloud-angled-rain", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}