import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowExpandDiagonal01Icon({ iconName = "arrow-expand-diagonal01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}