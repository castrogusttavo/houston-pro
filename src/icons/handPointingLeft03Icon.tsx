import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingLeft03Icon({ iconName = "hand-pointing-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}