import React from 'react';
import { Icon, IconProps } from '../Icon';

export const keyframe-align-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="keyframe-align-verticalicon" />
);