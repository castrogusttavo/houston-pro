import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-multiple01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-multiple01icon" />
);