import React from 'react';
import { Icon, IconProps } from '../Icon';

export const automotive-battery02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="automotive-battery02icon" />
);