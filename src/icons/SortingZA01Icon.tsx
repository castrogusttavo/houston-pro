import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortingZA01Icon({
  iconName = 'sorting-z-a01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
