import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Joystick04Icon({ iconName = "joystick04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}