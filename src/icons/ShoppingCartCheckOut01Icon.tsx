import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartCheckOut01Icon({ iconName = "shopping-cart-check-out01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}