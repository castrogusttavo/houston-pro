import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailVoice02Icon({
  iconName = 'mail-voice02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
