import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseLeftClick06Icon({
  iconName = 'mouse-left-click06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
