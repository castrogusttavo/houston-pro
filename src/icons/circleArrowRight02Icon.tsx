import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowRight02Icon({ iconName = "circle-arrow-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}