import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeUp05Icon({ iconName = "swipe-up05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}