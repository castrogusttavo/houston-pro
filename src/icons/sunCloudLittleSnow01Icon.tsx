import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudLittleSnow01Icon({ iconName = "sun-cloud-little-snow01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}