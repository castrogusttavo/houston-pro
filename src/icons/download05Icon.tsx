import React from 'react'
import { Icon, IconProps } from '../Icon'

export function download05Icon({ iconName = "download05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}