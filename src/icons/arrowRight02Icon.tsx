import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowRight02Icon({ iconName = "arrow-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}