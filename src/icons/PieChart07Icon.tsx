import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PieChart07Icon({
  iconName = 'pie-chart-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
