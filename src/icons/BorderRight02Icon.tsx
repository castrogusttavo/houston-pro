import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderRight02Icon({
  iconName = 'border-right02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
