import React from 'react';
import { Icon, IconProps } from '../Icon';

export const virtual-reality-vr02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="virtual-reality-vr02icon" />
);