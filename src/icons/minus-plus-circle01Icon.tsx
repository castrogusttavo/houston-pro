import React from 'react';
import { Icon, IconProps } from '../Icon';

export const minus-plus-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="minus-plus-circle01icon" />
);