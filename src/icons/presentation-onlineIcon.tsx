import React from 'react';
import { Icon, IconProps } from '../Icon';

export const presentation-onlineIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="presentation-onlineicon" />
);