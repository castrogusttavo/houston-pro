import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookshelf01Icon({ iconName = "bookshelf01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}