import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Exchange03Icon({ iconName = "exchange03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}