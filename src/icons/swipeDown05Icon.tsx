import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeDown05Icon({ iconName = "swipe-down05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}