import React from 'react'
import { Icon, IconProps } from '../Icon'

export function electricTower01Icon({ iconName = "electric-tower01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}