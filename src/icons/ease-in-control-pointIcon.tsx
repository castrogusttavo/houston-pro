import React from 'react';
import { Icon, IconProps } from '../Icon';

export const ease-in-control-pointIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="ease-in-control-pointicon" />
);