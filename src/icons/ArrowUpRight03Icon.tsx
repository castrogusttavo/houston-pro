import React from 'react';
import { Icon, IconProps } from '../Icon';

export function ArrowUpRight03Icon({
  iconName = 'arrow-up-right-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
