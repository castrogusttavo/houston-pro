import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookmark03Icon({ iconName = "bookmark03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}