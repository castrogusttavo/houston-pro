import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketSecure03Icon({ iconName = "shopping-basket-secure03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}