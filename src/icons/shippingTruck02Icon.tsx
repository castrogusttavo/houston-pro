import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shippingTruck02Icon({ iconName = "shipping-truck02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}