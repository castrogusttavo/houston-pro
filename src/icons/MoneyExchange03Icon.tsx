import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyExchange03Icon({
  iconName = 'money-exchange03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
