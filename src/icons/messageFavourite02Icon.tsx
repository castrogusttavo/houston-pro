import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageFavourite02Icon({ iconName = "message-favourite02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}