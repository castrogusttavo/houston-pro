import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchLocked02Icon({ iconName = "touch-locked02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}