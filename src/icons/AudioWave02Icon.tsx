import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AudioWave02Icon({
  iconName = 'audio-wave-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
