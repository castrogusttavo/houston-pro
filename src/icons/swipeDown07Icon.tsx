import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeDown07Icon({ iconName = "swipe-down07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}