import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TropicalStormTracks02Icon({ iconName = "tropical-storm-tracks02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}