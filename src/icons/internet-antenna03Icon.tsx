import React from 'react';
import { Icon, IconProps } from '../Icon';

export const internet-antenna03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="internet-antenna03icon" />
);