import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckOut02Icon({
  iconName = 'shopping-basket-check-out02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
