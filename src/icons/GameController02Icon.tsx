import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GameController02Icon({
  iconName = 'game-controller02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
