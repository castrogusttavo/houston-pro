import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-shrink01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-shrink01icon" />
);