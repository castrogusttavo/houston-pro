import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-horizontal-centerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-horizontal-centericon" />
);