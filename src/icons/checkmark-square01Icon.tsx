import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-square01icon" />
);