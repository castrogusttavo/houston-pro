import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowLeft02Icon({ iconName = "circle-arrow-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}