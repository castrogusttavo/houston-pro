import React from 'react';
import { Icon, IconProps } from '../Icon';

export const trapezoid-line-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="trapezoid-line-horizontalicon" />
);