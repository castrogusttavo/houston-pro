import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BitcoinSmartphone01Icon({ iconName = "bitcoin-smartphone01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}