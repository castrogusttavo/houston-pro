import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageCancel02Icon({ iconName = "message-cancel02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}