import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-right01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-right01icon" />
);