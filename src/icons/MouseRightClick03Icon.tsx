import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseRightClick03Icon({ iconName = "mouse-right-click03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}