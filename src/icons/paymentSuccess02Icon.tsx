import React from 'react'
import { Icon, IconProps } from '../Icon'

export function paymentSuccess02Icon({ iconName = "payment-success02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}