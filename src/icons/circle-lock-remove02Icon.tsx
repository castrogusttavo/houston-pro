import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-lock-remove02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-lock-remove02icon" />
);