import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowUpDoubleIcon({ iconName = "circle-arrow-up-double", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}