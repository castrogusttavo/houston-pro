import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settings05Icon({ iconName = "settings05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}