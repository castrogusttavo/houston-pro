import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AutomotiveBattery02Icon({
  iconName = 'automotive-battery02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
