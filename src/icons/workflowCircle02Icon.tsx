import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowCircle02Icon({ iconName = "workflow-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}