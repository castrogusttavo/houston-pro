import React from 'react'
import { Icon, IconProps } from '../Icon'

export function equipmentBenchPressIcon({ iconName = "equipment-bench-press", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}