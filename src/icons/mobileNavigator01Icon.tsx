import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mobileNavigator01Icon({ iconName = "mobile-navigator01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}