import React from 'react';
import { Icon, IconProps } from '../Icon';

export const pyramid-structure01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="pyramid-structure01icon" />
);