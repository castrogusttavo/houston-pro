import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketCheckOut03Icon({ iconName = "shopping-basket-check-out03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}