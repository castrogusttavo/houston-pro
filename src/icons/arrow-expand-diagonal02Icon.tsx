import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-expand-diagonal02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-expand-diagonal02icon" />
);