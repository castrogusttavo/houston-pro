import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-lock-check01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-lock-check01icon" />
);