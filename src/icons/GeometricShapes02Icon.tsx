import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GeometricShapes02Icon({
  iconName = 'geometric-shapes02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
