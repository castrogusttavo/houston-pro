import React from 'react'
import { Icon, IconProps } from '../Icon'

export function certificate02Icon({ iconName = "certificate02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}