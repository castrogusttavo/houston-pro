import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft02Icon({ iconName = "pointing-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}