import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle01Icon({
  iconName = 'workflow-circle01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
