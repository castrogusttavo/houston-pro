import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookmarkRemove02Icon({ iconName = "bookmark-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}