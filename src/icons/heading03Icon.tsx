import React from 'react'
import { Icon, IconProps } from '../Icon'

export function heading03Icon({ iconName = "heading03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}