import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowDataTransferVerticalIcon({ iconName = "circle-arrow-data-transfer-vertical", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}