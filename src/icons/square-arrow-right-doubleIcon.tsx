import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-right-doubleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-right-doubleicon" />
);