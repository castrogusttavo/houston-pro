import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageNotification02Icon({ iconName = "message-notification02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}