import React from 'react'
import { Icon, IconProps } from '../Icon'

export function underpants01Icon({ iconName = "underpants01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}