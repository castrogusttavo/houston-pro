import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleLockCheck02Icon({ iconName = "circle-lock-check02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}