import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageEdit02Icon({ iconName = "message-edit02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}