import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageUnlock02Icon({ iconName = "message-unlock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}