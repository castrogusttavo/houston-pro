import React from 'react'
import { Icon, IconProps } from '../Icon'

export function quillWrite02Icon({ iconName = "quill-write02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}