import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyAdd02Icon({ iconName = "money-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}