import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeRight03Icon({ iconName = "swipe-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}