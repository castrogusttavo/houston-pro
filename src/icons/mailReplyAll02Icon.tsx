import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailReplyAll02Icon({ iconName = "mail-reply-all02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}