import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-box-bottom-centerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-box-bottom-centericon" />
);