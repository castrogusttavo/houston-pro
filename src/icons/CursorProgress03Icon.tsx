import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorProgress03Icon({ iconName = "cursor-progress03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}