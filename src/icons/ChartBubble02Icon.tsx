import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ChartBubble02Icon({
  iconName = 'chart-bubble02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
