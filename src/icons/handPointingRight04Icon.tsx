import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingRight04Icon({ iconName = "hand-pointing-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}