import React from 'react';
import { Icon, IconProps } from '../Icon';

export const notification-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="notification-circleicon" />
);