import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rootFirstBrecketIcon({ iconName = "root-first-brecket", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}