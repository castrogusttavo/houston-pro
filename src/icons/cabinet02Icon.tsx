import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cabinet02Icon({ iconName = "cabinet02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}