import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyExchange01Icon({ iconName = "money-exchange01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}