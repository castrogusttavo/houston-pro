import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudAngledRain01Icon({ iconName = "sun-cloud-angled-rain01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}