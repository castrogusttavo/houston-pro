import React from 'react'
import { Icon, IconProps } from '../Icon'

export function computerProgramming02Icon({ iconName = "computer-programming02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}