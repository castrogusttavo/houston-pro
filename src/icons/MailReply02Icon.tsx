import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailReply02Icon({
  iconName = 'mail-reply02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
