import React from 'react'
import { Icon, IconProps } from '../Icon'

export function structure03Icon({ iconName = "structure03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}