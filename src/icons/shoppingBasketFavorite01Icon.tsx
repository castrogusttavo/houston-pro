import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketFavorite01Icon({ iconName = "shopping-basket-favorite01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}