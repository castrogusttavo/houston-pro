import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hierarchy-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hierarchy-circle01icon" />
);