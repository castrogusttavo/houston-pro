import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowSquare05Icon({ iconName = "workflow-square05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}