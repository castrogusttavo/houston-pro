import React from 'react';
import { Icon, IconProps } from '../Icon';

export const distribute-vertical-bottomIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="distribute-vertical-bottomicon" />
);