import React from 'react'
import { Icon, IconProps } from '../Icon'

export function navigator01Icon({ iconName = "navigator01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}