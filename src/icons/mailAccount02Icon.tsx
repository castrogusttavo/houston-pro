import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailAccount02Icon({ iconName = "mail-account02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}