import React from 'react';
import { Icon, IconProps } from '../Icon';

export const smartphone-lost-wifiIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="smartphone-lost-wifiicon" />
);