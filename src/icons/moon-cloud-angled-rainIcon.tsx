import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-angled-rainIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-angled-rainicon" />
);