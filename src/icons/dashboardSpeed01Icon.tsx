import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashboardSpeed01Icon({ iconName = "dashboard-speed01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}