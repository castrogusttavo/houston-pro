import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bitcoin-credit-cardIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bitcoin-credit-cardicon" />
);