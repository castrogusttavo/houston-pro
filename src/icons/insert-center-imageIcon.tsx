import React from 'react';
import { Icon, IconProps } from '../Icon';

export const insert-center-imageIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="insert-center-imageicon" />
);