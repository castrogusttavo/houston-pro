import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-down01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-down01icon" />
);