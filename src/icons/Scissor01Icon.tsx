import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Scissor01Icon({ iconName = 'scissor01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
