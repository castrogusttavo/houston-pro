import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-diagonal01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-diagonal01icon" />
);