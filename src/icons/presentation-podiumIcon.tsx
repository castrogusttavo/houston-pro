import React from 'react';
import { Icon, IconProps } from '../Icon';

export const presentation-podiumIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="presentation-podiumicon" />
);