import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp08Icon({ iconName = "swipe-up08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}