import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Parabola01Icon({ iconName = "parabola01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}