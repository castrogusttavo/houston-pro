import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarAdd01Icon({ iconName = "calendar-add01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}