import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-strikethroughIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-strikethroughicon" />
);