import React from 'react'
import { Icon, IconProps } from '../Icon'

export function gameController03Icon({ iconName = "game-controller03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}