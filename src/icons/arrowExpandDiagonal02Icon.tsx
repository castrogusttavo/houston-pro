import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowExpandDiagonal02Icon({ iconName = "arrow-expand-diagonal02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}