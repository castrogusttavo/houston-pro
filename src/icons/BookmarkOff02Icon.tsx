import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkOff02Icon({ iconName = "bookmark-off02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}