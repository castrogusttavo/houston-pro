import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailAtSign01Icon({ iconName = "mail-at-sign01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}