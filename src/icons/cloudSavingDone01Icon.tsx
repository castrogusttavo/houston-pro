import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cloudSavingDone01Icon({ iconName = "cloud-saving-done01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}