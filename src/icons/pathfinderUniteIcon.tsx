import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pathfinderUniteIcon({ iconName = "pathfinder-unite", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}