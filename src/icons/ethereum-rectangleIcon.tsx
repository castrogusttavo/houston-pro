import React from 'react';
import { Icon, IconProps } from '../Icon';

export const ethereum-rectangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="ethereum-rectangleicon" />
);