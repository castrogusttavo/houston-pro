import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Settings03Icon({ iconName = "settings03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}