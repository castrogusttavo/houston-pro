import React from 'react'
import { Icon, IconProps } from '../Icon'

export function agreement02Icon({ iconName = "agreement02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}