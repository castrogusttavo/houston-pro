import React from 'react';
import { Icon, IconProps } from '../Icon';

export const adobe-after-effectIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="adobe-after-effecticon" />
);