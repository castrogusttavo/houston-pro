import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationOffline04Icon({ iconName = "location-offline04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}