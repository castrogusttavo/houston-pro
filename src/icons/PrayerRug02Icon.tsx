import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PrayerRug02Icon({ iconName = "prayer-rug02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}