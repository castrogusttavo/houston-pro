import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryBox02Icon({ iconName = "delivery-box02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}