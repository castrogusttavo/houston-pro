import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-done03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-done03icon" />
);