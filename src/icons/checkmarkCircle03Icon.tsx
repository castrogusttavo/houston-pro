import React from 'react'
import { Icon, IconProps } from '../Icon'

export function checkmarkCircle03Icon({ iconName = "checkmark-circle03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}