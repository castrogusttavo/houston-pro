import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft05Icon({
  iconName = 'pointing-left05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
