import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailSecure02Icon({ iconName = "mail-secure02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}