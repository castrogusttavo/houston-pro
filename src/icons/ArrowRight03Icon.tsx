import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowRight03Icon({
  iconName = 'arrow-right03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
