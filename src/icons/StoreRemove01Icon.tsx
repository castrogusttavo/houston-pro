import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StoreRemove01Icon({
  iconName = 'store-remove01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
