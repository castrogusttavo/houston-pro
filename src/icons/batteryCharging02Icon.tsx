import React from 'react'
import { Icon, IconProps } from '../Icon'

export function batteryCharging02Icon({ iconName = "battery-charging02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}