import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-lock-check01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-lock-check01icon" />
);