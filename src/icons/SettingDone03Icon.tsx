import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingDone03Icon({ iconName = "setting-done03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}