import React from 'react'
import { Icon, IconProps } from '../Icon'

export function summation01Icon({ iconName = "summation01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}