import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrangeByLettersZAIcon({ iconName = "arrange-by-letters-z-a", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}