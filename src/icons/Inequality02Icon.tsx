import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Inequality02Icon({
  iconName = 'inequality02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
