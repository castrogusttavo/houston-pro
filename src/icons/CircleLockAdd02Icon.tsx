import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockAdd02Icon({
  iconName = 'circle-lock-add02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
