import React from 'react';
import { Icon, IconProps } from '../Icon';

export const curvy-left-right-directionIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="curvy-left-right-directionicon" />
);