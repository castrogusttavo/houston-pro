import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cardiogram02Icon({ iconName = "cardiogram02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}