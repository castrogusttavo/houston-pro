import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiContentGenerator01Icon({
  iconName = 'ai-content-generator-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
