import React from 'react';
import { Icon, IconProps } from '../Icon';

export const parking-area-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="parking-area-circleicon" />
);