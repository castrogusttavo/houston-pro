import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-square04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-square04icon" />
);