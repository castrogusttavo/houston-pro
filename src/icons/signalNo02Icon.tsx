import React from 'react'
import { Icon, IconProps } from '../Icon'

export function signalNo02Icon({ iconName = "signal-no02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}