import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorCircleSelection01Icon({
  iconName = 'cursor-circle-selection01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
