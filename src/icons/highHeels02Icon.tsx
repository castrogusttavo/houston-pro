import React from 'react'
import { Icon, IconProps } from '../Icon'

export function highHeels02Icon({ iconName = "high-heels02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}