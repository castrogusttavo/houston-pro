import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EarRings03Icon({
  iconName = 'ear-rings03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
