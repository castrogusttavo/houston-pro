import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cylinder03Icon({ iconName = "cylinder03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}