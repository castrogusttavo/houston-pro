import React from 'react'
import { Icon, IconProps } from '../Icon'

export function artificialIntelligence04Icon({ iconName = "artificial-intelligence04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}