import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-circle-settingsIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-circle-settingsicon" />
);