import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare10Icon({ iconName = "hierarchy-square10", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}