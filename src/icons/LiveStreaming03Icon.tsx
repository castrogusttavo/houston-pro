import React from 'react';
import { Icon, IconProps } from '../Icon';

export function LiveStreaming03Icon({
  iconName = 'live-streaming-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
