import React from 'react'
import { Icon, IconProps } from '../Icon'

export function configuration02Icon({ iconName = "configuration02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}