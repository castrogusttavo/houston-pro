import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapsGlobal02Icon({ iconName = "maps-global02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}