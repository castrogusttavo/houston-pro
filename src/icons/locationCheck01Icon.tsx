import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationCheck01Icon({ iconName = "location-check01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}