import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft05Icon({ iconName = "rotate-left05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}