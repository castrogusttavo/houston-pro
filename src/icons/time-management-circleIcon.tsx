import React from 'react';
import { Icon, IconProps } from '../Icon';

export const time-management-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="time-management-circleicon" />
);