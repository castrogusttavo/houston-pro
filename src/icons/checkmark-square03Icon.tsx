import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-square03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-square03icon" />
);