import React from 'react';
import { Icon, IconProps } from '../Icon';

export const remove-circle-half-dotIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="remove-circle-half-doticon" />
);