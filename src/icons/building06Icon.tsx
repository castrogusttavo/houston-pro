import React from 'react'
import { Icon, IconProps } from '../Icon'

export function building06Icon({ iconName = "building06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}