import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-align-justify-centerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-align-justify-centericon" />
);