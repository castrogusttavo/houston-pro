import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketRemove02Icon({ iconName = "shopping-basket-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}