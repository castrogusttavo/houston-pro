import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settingError04Icon({ iconName = "setting-error04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}