import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-left01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-left01icon" />
);