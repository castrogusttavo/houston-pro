import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiChemistry03Icon({
  iconName = 'ai-chemistry-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
