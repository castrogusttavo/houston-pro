import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-up-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-up-righticon" />
);