import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-angled-zap01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-angled-zap01icon" />
);