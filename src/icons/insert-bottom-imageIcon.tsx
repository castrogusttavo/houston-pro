import React from 'react';
import { Icon, IconProps } from '../Icon';

export const insert-bottom-imageIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="insert-bottom-imageicon" />
);