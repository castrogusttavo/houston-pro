import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-lock-minus01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-lock-minus01icon" />
);