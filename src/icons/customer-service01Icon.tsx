import React from 'react';
import { Icon, IconProps } from '../Icon';

export const customer-service01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="customer-service01icon" />
);