import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiSecurity03Icon({
  iconName = 'ai-security-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
