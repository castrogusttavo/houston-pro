import React from 'react';
import { Icon, IconProps } from '../Icon';

export const geometric-shapes01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="geometric-shapes01icon" />
);