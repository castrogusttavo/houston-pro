import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Calendar03Icon({
  iconName = 'calendar03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
