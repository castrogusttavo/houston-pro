import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Linkedin02Icon({ iconName = "linkedin02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}