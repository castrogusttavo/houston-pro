import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowShrink02Icon({ iconName = "circle-arrow-shrink02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}