import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settingDone04Icon({ iconName = "setting-done04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}