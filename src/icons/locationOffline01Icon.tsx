import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationOffline01Icon({ iconName = "location-offline01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}