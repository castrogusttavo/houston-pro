import React from 'react'
import { Icon, IconProps } from '../Icon'

export function elearningExchangeIcon({ iconName = "elearning-exchange", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}