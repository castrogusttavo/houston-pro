import React from 'react';
import { Icon, IconProps } from '../Icon';

export const keyframes-double-removeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="keyframes-double-removeicon" />
);