import React from 'react';
import { Icon, IconProps } from '../Icon';

export const tablet-connected-wifiIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="tablet-connected-wifiicon" />
);