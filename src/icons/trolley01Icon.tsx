import React from 'react'
import { Icon, IconProps } from '../Icon'

export function trolley01Icon({ iconName = "trolley01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}