import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight01Icon({ iconName = "rotate-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}