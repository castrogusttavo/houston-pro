import React from 'react'
import { Icon, IconProps } from '../Icon'

export function minusPlusCircle01Icon({ iconName = "minus-plus-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}