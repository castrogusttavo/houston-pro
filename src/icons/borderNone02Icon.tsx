import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderNone02Icon({ iconName = "border-none02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}