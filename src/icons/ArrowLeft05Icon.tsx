import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowLeft05Icon({
  iconName = 'arrow-left05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
