import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-offline02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-offline02icon" />
);