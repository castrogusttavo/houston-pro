import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarUpload01Icon({
  iconName = 'calendar-upload01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
