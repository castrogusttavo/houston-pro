import React from 'react'
import { Icon, IconProps } from '../Icon'

export function laptopPhoneSyncIcon({ iconName = "laptop-phone-sync", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}