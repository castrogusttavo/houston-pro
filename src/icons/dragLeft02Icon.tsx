import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragLeft02Icon({ iconName = "drag-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}