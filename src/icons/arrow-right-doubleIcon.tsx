import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-right-doubleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-right-doubleicon" />
);