import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-right01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-right01icon" />
);