import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDown04Icon({ iconName = "arrow-down04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}