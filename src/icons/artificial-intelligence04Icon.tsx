import React from 'react';
import { Icon, IconProps } from '../Icon';

export const artificial-intelligence04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="artificial-intelligence04icon" />
);