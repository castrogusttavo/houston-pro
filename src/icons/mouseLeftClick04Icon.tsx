import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mouseLeftClick04Icon({ iconName = "mouse-left-click04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}