import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mouse-right-click02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mouse-right-click02icon" />
);