import React from 'react'
import { Icon, IconProps } from '../Icon'

export function JusticeScale01Icon({ iconName = "justice-scale01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}