import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchLocked03Icon({ iconName = "touch-locked03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}