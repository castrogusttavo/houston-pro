import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare08Icon({ iconName = "hierarchy-square08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}