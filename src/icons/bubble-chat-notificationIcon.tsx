import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-notificationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-notificationicon" />
);