import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudSnow02Icon({ iconName = "sun-cloud-snow02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}