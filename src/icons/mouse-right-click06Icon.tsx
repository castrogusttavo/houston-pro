import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mouse-right-click06Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mouse-right-click06icon" />
);