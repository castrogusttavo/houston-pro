import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraMicrophone02Icon({ iconName = "camera-microphone02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}