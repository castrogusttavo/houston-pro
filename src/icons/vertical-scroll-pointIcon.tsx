import React from 'react';
import { Icon, IconProps } from '../Icon';

export const vertical-scroll-pointIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="vertical-scroll-pointicon" />
);