import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareUnlock01Icon({ iconName = "square-unlock01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}