import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookmark02Icon({ iconName = "bookmark02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}