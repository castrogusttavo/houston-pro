import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationUser04Icon({ iconName = "location-user04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}