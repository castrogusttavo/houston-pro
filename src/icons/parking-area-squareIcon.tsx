import React from 'react';
import { Icon, IconProps } from '../Icon';

export const parking-area-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="parking-area-squareicon" />
);