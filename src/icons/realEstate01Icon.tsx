import React from 'react'
import { Icon, IconProps } from '../Icon'

export function realEstate01Icon({ iconName = "real-estate01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}