import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-favorite01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-favorite01icon" />
);