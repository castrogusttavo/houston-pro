import React from 'react'
import { Icon, IconProps } from '../Icon'

export function eidMubarakIcon({ iconName = "eid-mubarak", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}