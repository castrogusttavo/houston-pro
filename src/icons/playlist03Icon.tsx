import React from 'react'
import { Icon, IconProps } from '../Icon'

export function playlist03Icon({ iconName = "playlist03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}