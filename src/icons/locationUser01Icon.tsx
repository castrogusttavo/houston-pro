import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationUser01Icon({ iconName = "location-user01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}