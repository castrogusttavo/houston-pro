import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArtificialIntelligence05Icon({
  iconName = 'artificial-intelligence05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
