import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userSettings02Icon({ iconName = "user-settings02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}