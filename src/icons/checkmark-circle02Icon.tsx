import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-circle02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-circle02icon" />
);