import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Redo03Icon({
  iconName = 'redo-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
