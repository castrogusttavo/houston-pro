import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-notification02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-notification02icon" />
);