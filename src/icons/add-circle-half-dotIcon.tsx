import React from 'react';
import { Icon, IconProps } from '../Icon';

export const add-circle-half-dotIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="add-circle-half-doticon" />
);