import React from 'react'
import { Icon, IconProps } from '../Icon'

export function penTool02Icon({ iconName = "pen-tool02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}