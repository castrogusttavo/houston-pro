import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-download02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-download02icon" />
);