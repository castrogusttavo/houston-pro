import React from 'react'
import { Icon, IconProps } from '../Icon'

export function uploadCircle02Icon({ iconName = "upload-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}