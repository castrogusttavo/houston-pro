import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DollarSend01Icon({ iconName = "dollar-send01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}