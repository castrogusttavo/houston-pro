import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mouse-left-click01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mouse-left-click01icon" />
);