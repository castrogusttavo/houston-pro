import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-reload-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-reload-verticalicon" />
);