import React from 'react';
import { Icon, IconProps } from '../Icon';

export const information-diamondIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="information-diamondicon" />
);