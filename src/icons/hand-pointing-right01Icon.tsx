import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-right01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-right01icon" />
);