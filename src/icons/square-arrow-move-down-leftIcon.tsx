import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-move-down-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-move-down-lefticon" />
);