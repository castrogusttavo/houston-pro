import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationStar01Icon({ iconName = "location-star01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}