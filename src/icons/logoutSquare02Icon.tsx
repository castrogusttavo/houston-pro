import React from 'react'
import { Icon, IconProps } from '../Icon'

export function logoutSquare02Icon({ iconName = "logout-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}