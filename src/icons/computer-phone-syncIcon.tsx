import React from 'react';
import { Icon, IconProps } from '../Icon';

export const computer-phone-syncIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="computer-phone-syncicon" />
);