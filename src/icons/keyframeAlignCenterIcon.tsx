import React from 'react'
import { Icon, IconProps } from '../Icon'

export function keyframeAlignCenterIcon({ iconName = "keyframe-align-center", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}