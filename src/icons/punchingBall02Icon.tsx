import React from 'react'
import { Icon, IconProps } from '../Icon'

export function punchingBall02Icon({ iconName = "punching-ball02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}