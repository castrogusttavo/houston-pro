import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraNightMode02Icon({ iconName = "camera-night-mode02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}