import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown08Icon({ iconName = "swipe-down08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}