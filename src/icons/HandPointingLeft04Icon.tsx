import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingLeft04Icon({ iconName = "hand-pointing-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}