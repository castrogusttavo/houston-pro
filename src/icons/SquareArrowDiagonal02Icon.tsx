import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDiagonal02Icon({
  iconName = 'square-arrow-diagonal02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
