import React from 'react'
import { Icon, IconProps } from '../Icon'

export function smartWatch03Icon({ iconName = "smart-watch03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}