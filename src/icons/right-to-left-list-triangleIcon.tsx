import React from 'react';
import { Icon, IconProps } from '../Icon';

export const right-to-left-list-triangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="right-to-left-list-triangleicon" />
);