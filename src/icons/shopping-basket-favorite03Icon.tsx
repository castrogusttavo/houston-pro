import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-favorite03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-favorite03icon" />
);