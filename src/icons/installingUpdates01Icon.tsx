import React from 'react'
import { Icon, IconProps } from '../Icon'

export function installingUpdates01Icon({ iconName = "installing-updates01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}