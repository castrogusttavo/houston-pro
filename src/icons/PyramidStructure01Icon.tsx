import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PyramidStructure01Icon({
  iconName = 'pyramid-structure01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
