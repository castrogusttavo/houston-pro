import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-move-left-downIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-move-left-downicon" />
);