import React from 'react';
import { Icon, IconProps } from '../Icon';

export const carousel-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="carousel-horizontalicon" />
);