import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settingDone01Icon({ iconName = "setting-done01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}