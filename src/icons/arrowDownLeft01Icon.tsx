import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDownLeft01Icon({ iconName = "arrow-down-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}