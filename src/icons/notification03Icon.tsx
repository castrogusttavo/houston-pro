import React from 'react'
import { Icon, IconProps } from '../Icon'

export function notification03Icon({ iconName = "notification03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}