import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-box-top-centerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-box-top-centericon" />
);