import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageDone02Icon({ iconName = "message-done02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}