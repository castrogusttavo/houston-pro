import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cupcake01Icon({ iconName = 'cupcake01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
