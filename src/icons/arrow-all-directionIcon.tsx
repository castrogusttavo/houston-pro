import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-all-directionIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-all-directionicon" />
);