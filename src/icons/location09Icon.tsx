import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location09Icon({ iconName = "location09", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}