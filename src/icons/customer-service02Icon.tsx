import React from 'react';
import { Icon, IconProps } from '../Icon';

export const customer-service02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="customer-service02icon" />
);