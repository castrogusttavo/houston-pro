import React from 'react';
import { Icon, IconProps } from '../Icon';

export const minus-plus-circle02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="minus-plus-circle02icon" />
);