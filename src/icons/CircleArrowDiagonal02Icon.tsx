import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDiagonal02Icon({
  iconName = 'circle-arrow-diagonal02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
