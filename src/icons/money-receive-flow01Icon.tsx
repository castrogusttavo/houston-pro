import React from 'react';
import { Icon, IconProps } from '../Icon';

export const money-receive-flow01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="money-receive-flow01icon" />
);