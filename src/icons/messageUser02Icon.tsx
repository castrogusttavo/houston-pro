import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageUser02Icon({ iconName = "message-user02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}