import React from 'react'
import { Icon, IconProps } from '../Icon'

export function building05Icon({ iconName = "building05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}