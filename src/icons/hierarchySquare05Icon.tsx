import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare05Icon({ iconName = "hierarchy-square05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}