import React from 'react';
import { Icon, IconProps } from '../Icon';

export const divide-sign-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="divide-sign-circleicon" />
);