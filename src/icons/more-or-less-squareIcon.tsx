import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-or-less-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-or-less-squareicon" />
);