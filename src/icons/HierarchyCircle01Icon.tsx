import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchyCircle01Icon({ iconName = "hierarchy-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}