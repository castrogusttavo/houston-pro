import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationAdd01Icon({
  iconName = 'location-add01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
