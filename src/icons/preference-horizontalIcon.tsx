import React from 'react';
import { Icon, IconProps } from '../Icon';

export const preference-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="preference-horizontalicon" />
);