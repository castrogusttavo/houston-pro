import React from 'react';
import { Icon, IconProps } from '../Icon';

export const curvy-right-directionIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="curvy-right-directionicon" />
);