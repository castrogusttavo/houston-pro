import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryDelay02Icon({ iconName = "delivery-delay02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}