import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sustainable-energyIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sustainable-energyicon" />
);