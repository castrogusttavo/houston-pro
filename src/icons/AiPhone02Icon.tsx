import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiPhone02Icon({ iconName = "ai-phone02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}