import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationOffline02Icon({ iconName = "location-offline02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}