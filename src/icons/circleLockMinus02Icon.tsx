import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleLockMinus02Icon({ iconName = "circle-lock-minus02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}