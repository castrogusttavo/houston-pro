import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location05Icon({ iconName = "location05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}