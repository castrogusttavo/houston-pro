import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowShrink02Icon({ iconName = "square-arrow-shrink02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}