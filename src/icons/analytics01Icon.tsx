import React from 'react'
import { Icon, IconProps } from '../Icon'

export function analytics01Icon({ iconName = "analytics01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}