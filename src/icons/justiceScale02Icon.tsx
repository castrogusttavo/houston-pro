import React from 'react'
import { Icon, IconProps } from '../Icon'

export function justiceScale02Icon({ iconName = "justice-scale02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}