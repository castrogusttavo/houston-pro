import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaleTag02Icon({ iconName = "sale-tag02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}