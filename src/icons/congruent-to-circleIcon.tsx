import React from 'react';
import { Icon, IconProps } from '../Icon';

export const congruent-to-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="congruent-to-circleicon" />
);