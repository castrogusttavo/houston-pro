import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-rectangle-selection01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-rectangle-selection01icon" />
);