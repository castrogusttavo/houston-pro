import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserRemove02Icon({ iconName = "user-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}