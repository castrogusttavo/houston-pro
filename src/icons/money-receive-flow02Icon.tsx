import React from 'react';
import { Icon, IconProps } from '../Icon';

export const money-receive-flow02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="money-receive-flow02icon" />
);