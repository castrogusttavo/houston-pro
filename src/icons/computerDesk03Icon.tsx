import React from 'react'
import { Icon, IconProps } from '../Icon'

export function computerDesk03Icon({ iconName = "computer-desk03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}