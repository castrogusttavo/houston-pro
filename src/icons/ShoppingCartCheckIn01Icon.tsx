import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartCheckIn01Icon({ iconName = "shopping-cart-check-in01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}