import React from 'react';
import { Icon, IconProps } from '../Icon';

export const internet-antenna04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="internet-antenna04icon" />
);