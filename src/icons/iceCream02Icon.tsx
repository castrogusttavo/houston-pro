import React from 'react'
import { Icon, IconProps } from '../Icon'

export function iceCream02Icon({ iconName = "ice-cream02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}