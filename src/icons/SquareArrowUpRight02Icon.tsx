import React from 'react';
import { Icon, IconProps } from '../Icon';

export function SquareArrowUpRight02Icon({
  iconName = 'square-arrow-up-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
