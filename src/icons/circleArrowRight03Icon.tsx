import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowRight03Icon({ iconName = "circle-arrow-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}