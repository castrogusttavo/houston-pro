import React from 'react';
import { Icon, IconProps } from '../Icon';

export const trapezoid-line-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="trapezoid-line-verticalicon" />
);