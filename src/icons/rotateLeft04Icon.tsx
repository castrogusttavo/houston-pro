import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft04Icon({ iconName = "rotate-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}