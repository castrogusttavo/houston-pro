import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarLock02Icon({ iconName = "calendar-lock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}