import React from 'react'
import { Icon, IconProps } from '../Icon'

export function satellite01Icon({ iconName = "satellite01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}