import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasket03Icon({ iconName = "shopping-basket03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}