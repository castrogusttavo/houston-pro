import React from 'react';
import { Icon, IconProps } from '../Icon';

export const tropical-storm-tracksIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="tropical-storm-tracksicon" />
);