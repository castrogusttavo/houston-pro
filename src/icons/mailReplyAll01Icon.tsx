import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailReplyAll01Icon({ iconName = "mail-reply-all01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}