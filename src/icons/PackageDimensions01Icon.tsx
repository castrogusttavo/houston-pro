import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PackageDimensions01Icon({
  iconName = 'package-dimensions-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
