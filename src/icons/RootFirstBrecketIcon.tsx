import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RootFirstBrecketIcon({
  iconName = 'root-first-brecket',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
