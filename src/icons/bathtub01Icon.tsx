import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bathtub01Icon({ iconName = "bathtub01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}