import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorMagicSelection02Icon({ iconName = "cursor-magic-selection02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}