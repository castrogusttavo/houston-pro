import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingRight04Icon({ iconName = "pointing-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}