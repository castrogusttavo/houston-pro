import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowCircle04Icon({ iconName = "workflow-circle04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}