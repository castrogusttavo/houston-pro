import React from 'react'
import { Icon, IconProps } from '../Icon'

export function candelier02Icon({ iconName = "candelier02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}