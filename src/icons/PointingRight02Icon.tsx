import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight02Icon({
  iconName = 'pointing-right02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
