import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraSmile02Icon({ iconName = "camera-smile02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}