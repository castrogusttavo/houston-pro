import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageAdd02Icon({ iconName = "message-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}