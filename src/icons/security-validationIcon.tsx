import React from 'react';
import { Icon, IconProps } from '../Icon';

export const security-validationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="security-validationicon" />
);