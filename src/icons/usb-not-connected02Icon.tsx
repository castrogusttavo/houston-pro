import React from 'react';
import { Icon, IconProps } from '../Icon';

export const usb-not-connected02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="usb-not-connected02icon" />
);