import React from 'react'
import { Icon, IconProps } from '../Icon'

export function knowledge02Icon({ iconName = "knowledge02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}