import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-diagonal02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-diagonal02icon" />
);