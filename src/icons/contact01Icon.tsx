import React from 'react'
import { Icon, IconProps } from '../Icon'

export function contact01Icon({ iconName = "contact01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}