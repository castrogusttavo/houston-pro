import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowUp01Icon({
  iconName = 'circle-arrow-up01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
