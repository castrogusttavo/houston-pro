import React from 'react'
import { Icon, IconProps } from '../Icon'

export function fourFinger02Icon({ iconName = "four-finger02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}