import React from 'react'
import { Icon, IconProps } from '../Icon'

export function minusPlusSquare01Icon({ iconName = "minus-plus-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}