import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapsLocation02Icon({ iconName = "maps-location02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}