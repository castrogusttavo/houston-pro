import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase04Icon({ iconName = "briefcase04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}