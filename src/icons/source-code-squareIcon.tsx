import React from 'react';
import { Icon, IconProps } from '../Icon';

export const source-code-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="source-code-squareicon" />
);