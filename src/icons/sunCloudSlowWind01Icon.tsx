import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudSlowWind01Icon({ iconName = "sun-cloud-slow-wind01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}