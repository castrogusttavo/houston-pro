import React from 'react'
import { Icon, IconProps } from '../Icon'

export function knowledge01Icon({ iconName = "knowledge01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}