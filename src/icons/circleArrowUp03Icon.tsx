import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowUp03Icon({ iconName = "circle-arrow-up03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}