import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loginCircle01Icon({ iconName = "login-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}