import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight08Icon({ iconName = "pointing-right08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}