import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BedSingle02Icon({ iconName = "bed-single02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}