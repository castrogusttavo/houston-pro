import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationUser02Icon({ iconName = "location-user02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}