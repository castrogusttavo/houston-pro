import React from 'react'
import { Icon, IconProps } from '../Icon'

export function maximize02Icon({ iconName = "maximize02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}