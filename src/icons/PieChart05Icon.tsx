import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PieChart05Icon({
  iconName = 'pie-chart-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
