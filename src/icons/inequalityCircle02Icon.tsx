import React from 'react'
import { Icon, IconProps } from '../Icon'

export function inequalityCircle02Icon({ iconName = "inequality-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}