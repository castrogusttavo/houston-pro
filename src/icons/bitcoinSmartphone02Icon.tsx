import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bitcoinSmartphone02Icon({ iconName = "bitcoin-smartphone02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}