import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wallet-not-found02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wallet-not-found02icon" />
);