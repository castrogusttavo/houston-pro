import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowLeft03Icon({
  iconName = 'circle-arrow-left03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
