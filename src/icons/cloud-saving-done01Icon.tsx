import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cloud-saving-done01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cloud-saving-done01icon" />
);