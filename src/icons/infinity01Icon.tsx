import React from 'react'
import { Icon, IconProps } from '../Icon'

export function infinity01Icon({ iconName = "infinity01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}