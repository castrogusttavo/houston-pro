import React from 'react'
import { Icon, IconProps } from '../Icon'

export function imageRemove01Icon({ iconName = "image-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}