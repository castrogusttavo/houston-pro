import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraSmile01Icon({ iconName = "camera-smile01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}