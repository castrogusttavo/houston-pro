import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settingError03Icon({ iconName = "setting-error03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}