import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowDown03Icon({ iconName = "square-arrow-down03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}