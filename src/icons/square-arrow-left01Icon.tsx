import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-left01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-left01icon" />
);