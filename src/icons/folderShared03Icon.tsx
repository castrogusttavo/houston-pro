import React from 'react'
import { Icon, IconProps } from '../Icon'

export function folderShared03Icon({ iconName = "folder-shared03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}