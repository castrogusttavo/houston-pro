import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UploadSquare02Icon({
  iconName = 'upload-square02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
