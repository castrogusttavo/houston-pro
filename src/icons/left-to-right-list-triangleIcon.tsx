import React from 'react';
import { Icon, IconProps } from '../Icon';

export const left-to-right-list-triangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="left-to-right-list-triangleicon" />
);