import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-hailstone01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-hailstone01icon" />
);