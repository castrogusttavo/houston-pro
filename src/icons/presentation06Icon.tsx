import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentation06Icon({ iconName = "presentation06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}