import React from 'react';
import { Icon, IconProps } from '../Icon';

export const the-prophets-mosqueIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="the-prophets-mosqueicon" />
);