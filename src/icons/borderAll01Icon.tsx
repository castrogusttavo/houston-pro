import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderAll01Icon({ iconName = "border-all01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}