import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare02Icon({ iconName = "hierarchy-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}