import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mouseRightClick05Icon({ iconName = "mouse-right-click05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}