import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashboardSquare02Icon({ iconName = "dashboard-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}