import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MobileNavigator02Icon({ iconName = "mobile-navigator02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}