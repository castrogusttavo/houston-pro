import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bitcoin-piggy-bankIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bitcoin-piggy-bankicon" />
);