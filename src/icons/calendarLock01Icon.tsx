import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarLock01Icon({ iconName = "calendar-lock01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}