import React from 'react'
import { Icon, IconProps } from '../Icon'

export function playListFavourite02Icon({ iconName = "play-list-favourite02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}