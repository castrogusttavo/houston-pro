import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-reload01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-reload01icon" />
);