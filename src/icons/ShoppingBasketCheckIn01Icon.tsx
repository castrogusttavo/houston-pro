import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckIn01Icon({
  iconName = 'shopping-basket-check-in01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
