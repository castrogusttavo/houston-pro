import React from 'react'
import { Icon, IconProps } from '../Icon'

export function summation02Icon({ iconName = "summation02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}