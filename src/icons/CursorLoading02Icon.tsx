import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorLoading02Icon({
  iconName = 'cursor-loading02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
