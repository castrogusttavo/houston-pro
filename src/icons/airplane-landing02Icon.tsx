import React from 'react';
import { Icon, IconProps } from '../Icon';

export const airplane-landing02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="airplane-landing02icon" />
);