import React from 'react'
import { Icon, IconProps } from '../Icon'

export function facebook02Icon({ iconName = "facebook02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}