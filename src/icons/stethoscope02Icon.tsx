import React from 'react'
import { Icon, IconProps } from '../Icon'

export function stethoscope02Icon({ iconName = "stethoscope02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}