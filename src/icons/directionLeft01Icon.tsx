import React from 'react'
import { Icon, IconProps } from '../Icon'

export function directionLeft01Icon({ iconName = "direction-left01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}