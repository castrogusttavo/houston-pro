import React from 'react'
import { Icon, IconProps } from '../Icon'

export function musicNote03Icon({ iconName = "music-note03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}