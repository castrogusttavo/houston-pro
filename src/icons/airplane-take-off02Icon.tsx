import React from 'react';
import { Icon, IconProps } from '../Icon';

export const airplane-take-off02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="airplane-take-off02icon" />
);