import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-little-snow01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-little-snow01icon" />
);