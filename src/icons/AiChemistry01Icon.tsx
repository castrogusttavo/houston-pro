import React from 'react';
import { Icon, IconProps } from '../Icon';

export function AiChemistry01Icon({
  iconName = 'ai-chemistry-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
