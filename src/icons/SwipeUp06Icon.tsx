import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp06Icon({
  iconName = 'swipe-up06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
