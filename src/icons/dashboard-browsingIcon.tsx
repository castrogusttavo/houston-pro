import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-browsingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-browsingicon" />
);