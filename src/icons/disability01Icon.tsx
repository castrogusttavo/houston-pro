import React from 'react'
import { Icon, IconProps } from '../Icon'

export function disability01Icon({ iconName = "disability01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}