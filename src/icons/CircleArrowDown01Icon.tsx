import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDown01Icon({ iconName = "circle-arrow-down01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}