import React from 'react'
import { Icon, IconProps } from '../Icon'

export function building03Icon({ iconName = "building03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}