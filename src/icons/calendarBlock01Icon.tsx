import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarBlock01Icon({ iconName = "calendar-block01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}