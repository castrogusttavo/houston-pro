import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bitcoin04Icon({ iconName = "bitcoin04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}