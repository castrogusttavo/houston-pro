import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookshelf02Icon({ iconName = "bookshelf02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}