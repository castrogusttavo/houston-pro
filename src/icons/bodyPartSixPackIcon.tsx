import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bodyPartSixPackIcon({ iconName = "body-part-six-pack", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}