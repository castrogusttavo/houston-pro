import React from 'react';
import { Icon, IconProps } from '../Icon';

export const checkmark-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="checkmark-circle01icon" />
);