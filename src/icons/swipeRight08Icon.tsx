import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeRight08Icon({ iconName = "swipe-right08", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}