import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userLove02Icon({ iconName = "user-love02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}