import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mouseRightClick04Icon({ iconName = "mouse-right-click04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}