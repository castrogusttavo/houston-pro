import React from 'react'
import { Icon, IconProps } from '../Icon'

export function parabola03Icon({ iconName = "parabola03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}