import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailRemove01Icon({ iconName = "mail-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}