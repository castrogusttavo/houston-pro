import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowUp03Icon({ iconName = "square-arrow-up03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}