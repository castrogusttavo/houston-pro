import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowRight01Icon({
  iconName = 'circle-arrow-right01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
