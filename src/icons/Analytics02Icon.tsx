import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Analytics02Icon({
  iconName = 'analytics02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
