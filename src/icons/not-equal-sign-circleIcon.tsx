import React from 'react';
import { Icon, IconProps } from '../Icon';

export const not-equal-sign-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="not-equal-sign-circleicon" />
);