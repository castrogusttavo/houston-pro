import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shareLocation01Icon({ iconName = "share-location01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}