import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-favourite01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-favourite01icon" />
);