import React from 'react'
import { Icon, IconProps } from '../Icon'

export function signalLow02Icon({ iconName = "signal-low02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}