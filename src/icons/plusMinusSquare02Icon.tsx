import React from 'react'
import { Icon, IconProps } from '../Icon'

export function plusMinusSquare02Icon({ iconName = "plus-minus-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}