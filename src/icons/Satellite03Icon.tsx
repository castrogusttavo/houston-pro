import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Satellite03Icon({
  iconName = 'satellite03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
