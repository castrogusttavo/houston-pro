import React from 'react'
import { Icon, IconProps } from '../Icon'

export function systemUpdate01Icon({ iconName = "system-update01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}