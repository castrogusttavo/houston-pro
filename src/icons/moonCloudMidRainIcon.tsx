import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudMidRainIcon({ iconName = "moon-cloud-mid-rain", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}