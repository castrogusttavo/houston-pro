import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sorting05Icon({ iconName = "sorting05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}