import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ParagraphBulletsPoint01Icon({ iconName = "paragraph-bullets-point01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}