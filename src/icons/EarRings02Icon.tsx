import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EarRings02Icon({ iconName = "ear-rings02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}