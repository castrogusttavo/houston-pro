import React from 'react';
import { Icon, IconProps } from '../Icon';

export const left-to-right-block-quoteIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="left-to-right-block-quoteicon" />
);