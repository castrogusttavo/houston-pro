import React from 'react'
import { Icon, IconProps } from '../Icon'

export function workflowCircle03Icon({ iconName = "workflow-circle03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}