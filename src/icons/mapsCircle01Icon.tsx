import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mapsCircle01Icon({ iconName = "maps-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}