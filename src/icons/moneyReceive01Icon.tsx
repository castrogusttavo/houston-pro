import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyReceive01Icon({ iconName = "money-receive01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}