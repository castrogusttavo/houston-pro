import React from 'react'
import { Icon, IconProps } from '../Icon'

export function storeRemove02Icon({ iconName = "store-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}