import React from 'react'
import { Icon, IconProps } from '../Icon'

export function saveEnergy01Icon({ iconName = "save-energy01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}