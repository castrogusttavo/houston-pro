import React from 'react'
import { Icon, IconProps } from '../Icon'

export function storeVerified02Icon({ iconName = "store-verified02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}