import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeRight02Icon({ iconName = "swipe-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}