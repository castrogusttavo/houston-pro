import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-add-selection01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-add-selection01icon" />
);