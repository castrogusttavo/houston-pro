import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorRemoveSelection02Icon({ iconName = "cursor-remove-selection02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}