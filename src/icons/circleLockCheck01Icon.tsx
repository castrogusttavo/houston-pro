import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleLockCheck01Icon({ iconName = "circle-lock-check01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}