import React from 'react';
import { Icon, IconProps } from '../Icon';

export const geometric-shapes02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="geometric-shapes02icon" />
);