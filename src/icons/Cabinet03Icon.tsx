import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cabinet03Icon({ iconName = "cabinet03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}