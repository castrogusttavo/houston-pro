import React from 'react'
import { Icon, IconProps } from '../Icon'

export function victoryFinger03Icon({ iconName = "victory-finger03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}