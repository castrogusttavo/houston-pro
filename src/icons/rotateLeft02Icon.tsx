import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft02Icon({ iconName = "rotate-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}