import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CardExchange02Icon({ iconName = "card-exchange02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}