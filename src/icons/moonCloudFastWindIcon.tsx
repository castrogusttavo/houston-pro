import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudFastWindIcon({ iconName = "moon-cloud-fast-wind", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}