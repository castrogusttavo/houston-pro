import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-horizontal-circle02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-horizontal-circle02icon" />
);