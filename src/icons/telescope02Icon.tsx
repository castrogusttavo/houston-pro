import React from 'react'
import { Icon, IconProps } from '../Icon'

export function telescope02Icon({ iconName = "telescope02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}