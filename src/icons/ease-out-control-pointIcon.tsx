import React from 'react';
import { Icon, IconProps } from '../Icon';

export const ease-out-control-pointIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="ease-out-control-pointicon" />
);