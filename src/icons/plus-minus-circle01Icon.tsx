import React from 'react';
import { Icon, IconProps } from '../Icon';

export const plus-minus-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="plus-minus-circle01icon" />
);