import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loveKoreanFingerIcon({ iconName = "love-korean-finger", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}