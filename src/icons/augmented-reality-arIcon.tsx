import React from 'react';
import { Icon, IconProps } from '../Icon';

export const augmented-reality-arIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="augmented-reality-aricon" />
);