import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarSetting02Icon({ iconName = "calendar-setting02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}