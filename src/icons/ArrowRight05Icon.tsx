import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowRight05Icon({
  iconName = 'arrow-right05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
