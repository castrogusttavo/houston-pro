import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare04Icon({ iconName = "hierarchy-square04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}