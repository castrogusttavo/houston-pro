import React from 'react'
import { Icon, IconProps } from '../Icon'

export function profile02Icon({ iconName = "profile02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}