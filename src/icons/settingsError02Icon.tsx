import React from 'react'
import { Icon, IconProps } from '../Icon'

export function settingsError02Icon({ iconName = "settings-error02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}