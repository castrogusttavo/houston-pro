import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rssConnected02Icon({ iconName = "rss-connected02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}