import React from 'react';
import { Icon, IconProps } from '../Icon';

export const installing-updates01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="installing-updates01icon" />
);