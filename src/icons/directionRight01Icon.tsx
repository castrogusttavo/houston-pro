import React from 'react'
import { Icon, IconProps } from '../Icon'

export function directionRight01Icon({ iconName = "direction-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}