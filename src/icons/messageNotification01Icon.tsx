import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageNotification01Icon({ iconName = "message-notification01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}