import React from 'react';
import { Icon, IconProps } from '../Icon';

export const dashboard-circle-addIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="dashboard-circle-addicon" />
);