import React from 'react';
import { Icon, IconProps } from '../Icon';

export const battery-eco-chargingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="battery-eco-chargingicon" />
);