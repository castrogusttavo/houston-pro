import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-angled-rain-zapIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-angled-rain-zapicon" />
);