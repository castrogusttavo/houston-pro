import React from 'react'
import { Icon, IconProps } from '../Icon'

export function linkSquare01Icon({ iconName = "link-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}