import React from 'react';
import { Icon, IconProps } from '../Icon';

export function ComputerTerminal02Icon({
  iconName = 'computer-terminal-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
