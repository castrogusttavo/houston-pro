import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare06Icon({ iconName = "hierarchy-square06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}