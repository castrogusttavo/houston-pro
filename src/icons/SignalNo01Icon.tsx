import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SignalNo01Icon({ iconName = "signal-no01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}