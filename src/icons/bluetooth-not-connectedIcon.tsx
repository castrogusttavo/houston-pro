import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bluetooth-not-connectedIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bluetooth-not-connectedicon" />
);