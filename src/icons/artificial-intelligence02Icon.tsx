import React from 'react';
import { Icon, IconProps } from '../Icon';

export const artificial-intelligence02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="artificial-intelligence02icon" />
);