import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tickDouble02Icon({ iconName = "tick-double02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}