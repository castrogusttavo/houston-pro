import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-done02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-done02icon" />
);