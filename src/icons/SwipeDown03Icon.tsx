import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown03Icon({
  iconName = 'swipe-down03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
