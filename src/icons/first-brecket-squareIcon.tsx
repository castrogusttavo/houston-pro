import React from 'react';
import { Icon, IconProps } from '../Icon';

export const first-brecket-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="first-brecket-squareicon" />
);