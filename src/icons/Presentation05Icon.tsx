import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Presentation05Icon({ iconName = "presentation05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}