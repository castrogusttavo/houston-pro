import React from 'react';
import { Icon, IconProps } from '../Icon';

export function ServerStack02Icon({
  iconName = 'server-stack-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
