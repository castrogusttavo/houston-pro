import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SignalMedium02Icon({ iconName = "signal-medium02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}