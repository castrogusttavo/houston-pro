import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailUpload02Icon({
  iconName = 'mail-upload02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
