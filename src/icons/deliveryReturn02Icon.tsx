import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryReturn02Icon({ iconName = "delivery-return02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}