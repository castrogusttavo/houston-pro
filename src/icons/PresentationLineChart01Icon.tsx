import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PresentationLineChart01Icon({ iconName = "presentation-line-chart01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}