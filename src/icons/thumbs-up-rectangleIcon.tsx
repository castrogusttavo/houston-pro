import React from 'react';
import { Icon, IconProps } from '../Icon';

export const thumbs-up-rectangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="thumbs-up-rectangleicon" />
);