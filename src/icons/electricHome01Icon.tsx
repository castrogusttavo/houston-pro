import React from 'react'
import { Icon, IconProps } from '../Icon'

export function electricHome01Icon({ iconName = "electric-home01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}