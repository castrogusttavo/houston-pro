import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareLockRemove01Icon({ iconName = "square-lock-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}