import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragRight02Icon({ iconName = "drag-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}