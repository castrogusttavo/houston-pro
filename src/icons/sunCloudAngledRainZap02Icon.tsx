import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudAngledRainZap02Icon({ iconName = "sun-cloud-angled-rain-zap02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}