import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PhoneOff01Icon({
  iconName = 'phone-off01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
