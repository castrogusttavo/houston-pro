import React from 'react'
import { Icon, IconProps } from '../Icon'

export function lassoTool02Icon({ iconName = "lasso-tool02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}