import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Setting06Icon({ iconName = "setting06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}