import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-expand02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-expand02icon" />
);