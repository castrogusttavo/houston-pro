import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchInteraction03Icon({ iconName = "touch-interaction03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}