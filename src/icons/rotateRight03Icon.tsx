import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateRight03Icon({ iconName = "rotate-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}