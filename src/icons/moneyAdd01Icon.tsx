import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyAdd01Icon({ iconName = "money-add01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}