import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-favorite02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-favorite02icon" />
);