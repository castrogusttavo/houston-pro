import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloud02Icon({ iconName = "sun-cloud02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}