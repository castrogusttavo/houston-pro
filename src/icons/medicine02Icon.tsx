import React from 'react'
import { Icon, IconProps } from '../Icon'

export function medicine02Icon({ iconName = "medicine02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}