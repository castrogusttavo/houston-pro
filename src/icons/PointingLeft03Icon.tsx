import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft03Icon({ iconName = "pointing-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}