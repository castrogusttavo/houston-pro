import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArtificialIntelligence06Icon({
  iconName = 'artificial-intelligence06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
