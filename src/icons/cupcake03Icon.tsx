import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cupcake03Icon({ iconName = "cupcake03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}