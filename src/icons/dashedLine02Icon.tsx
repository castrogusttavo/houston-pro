import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashedLine02Icon({ iconName = "dashed-line02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}