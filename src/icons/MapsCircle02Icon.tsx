import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsCircle02Icon({
  iconName = 'maps-circle02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
