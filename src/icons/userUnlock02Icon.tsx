import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userUnlock02Icon({ iconName = "user-unlock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}