import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudAngledZap02Icon({ iconName = "sun-cloud-angled-zap02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}