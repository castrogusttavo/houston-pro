import React from 'react'
import { Icon, IconProps } from '../Icon'

export function repeatOne01Icon({ iconName = "repeat-one01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}