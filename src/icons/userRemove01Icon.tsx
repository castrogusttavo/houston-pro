import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userRemove01Icon({ iconName = "user-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}