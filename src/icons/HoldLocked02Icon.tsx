import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HoldLocked02Icon({
  iconName = 'hold-locked02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
