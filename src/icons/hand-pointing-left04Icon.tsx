import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-left04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-left04icon" />
);