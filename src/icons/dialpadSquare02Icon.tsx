import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dialpadSquare02Icon({ iconName = "dialpad-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}