import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlusMinusCircle02Icon({ iconName = "plus-minus-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}