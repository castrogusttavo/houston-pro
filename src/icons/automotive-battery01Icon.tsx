import React from 'react';
import { Icon, IconProps } from '../Icon';

export const automotive-battery01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="automotive-battery01icon" />
);