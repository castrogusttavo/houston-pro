import React from 'react'
import { Icon, IconProps } from '../Icon'

export function taskRemove02Icon({ iconName = "task-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}