import React from 'react';
import { Icon, IconProps } from '../Icon';

export const password-validationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="password-validationicon" />
);