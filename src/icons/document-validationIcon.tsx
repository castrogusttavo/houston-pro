import React from 'react';
import { Icon, IconProps } from '../Icon';

export const document-validationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="document-validationicon" />
);