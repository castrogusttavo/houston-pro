import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dollarSend02Icon({ iconName = "dollar-send02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}