import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userShield01Icon({ iconName = "user-shield01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}