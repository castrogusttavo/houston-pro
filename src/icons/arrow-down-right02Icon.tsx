import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-down-right02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-down-right02icon" />
);