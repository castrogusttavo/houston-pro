import React from 'react';
import { Icon, IconProps } from '../Icon';

export const credit-card-changeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="credit-card-changeicon" />
);