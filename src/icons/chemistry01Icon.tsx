import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chemistry01Icon({ iconName = "chemistry01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}