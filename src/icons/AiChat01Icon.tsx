import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiChat01Icon({ iconName = 'ai-chat01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
