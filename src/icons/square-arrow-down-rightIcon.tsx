import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-down-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-down-righticon" />
);