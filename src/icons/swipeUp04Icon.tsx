import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeUp04Icon({ iconName = "swipe-up04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}