import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chartLineData02Icon({ iconName = "chart-line-data02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}