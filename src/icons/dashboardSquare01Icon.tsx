import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashboardSquare01Icon({ iconName = "dashboard-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}