import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeUp01Icon({ iconName = "swipe-up01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}