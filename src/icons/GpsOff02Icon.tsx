import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GpsOff02Icon({ iconName = 'gps-off02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
