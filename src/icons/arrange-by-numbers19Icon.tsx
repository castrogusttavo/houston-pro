import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrange-by-numbers19Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrange-by-numbers19icon" />
);