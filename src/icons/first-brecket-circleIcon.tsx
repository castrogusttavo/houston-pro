import React from 'react';
import { Icon, IconProps } from '../Icon';

export const first-brecket-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="first-brecket-circleicon" />
);