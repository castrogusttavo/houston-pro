import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PencilEdit02Icon({ iconName = "pencil-edit02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}