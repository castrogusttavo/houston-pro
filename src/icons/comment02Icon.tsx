import React from 'react'
import { Icon, IconProps } from '../Icon'

export function comment02Icon({ iconName = "comment02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}