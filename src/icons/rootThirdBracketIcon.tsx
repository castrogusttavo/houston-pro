import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rootThirdBracketIcon({ iconName = "root-third-bracket", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}