import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudSnow01Icon({ iconName = "sun-cloud-snow01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}