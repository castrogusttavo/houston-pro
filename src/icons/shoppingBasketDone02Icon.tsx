import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketDone02Icon({ iconName = "shopping-basket-done02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}