import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeLeft04Icon({ iconName = "swipe-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}