import React from 'react'
import { Icon, IconProps } from '../Icon'

export function frenchFries02Icon({ iconName = "french-fries02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}