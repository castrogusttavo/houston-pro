import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudSlowWind02Icon({ iconName = "sun-cloud-slow-wind02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}