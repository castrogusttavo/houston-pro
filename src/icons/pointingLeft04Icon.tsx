import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft04Icon({ iconName = "pointing-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}