import React from 'react'
import { Icon, IconProps } from '../Icon'

export function briefcase03Icon({ iconName = "briefcase03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}