import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-add03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-add03icon" />
);