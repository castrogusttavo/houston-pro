import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateLeft06Icon({ iconName = "rotate-left06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}