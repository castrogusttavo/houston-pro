import React from 'react'
import { Icon, IconProps } from '../Icon'

export function location07Icon({ iconName = "location07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}