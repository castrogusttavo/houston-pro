import React from 'react'
import { Icon, IconProps } from '../Icon'

export function roadLocation01Icon({ iconName = "road-location01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}