import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blockchain07Icon({ iconName = "blockchain07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}