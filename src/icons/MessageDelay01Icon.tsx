import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageDelay01Icon({ iconName = "message-delay01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}