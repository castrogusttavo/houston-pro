import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookshelf03Icon({ iconName = "bookshelf03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}