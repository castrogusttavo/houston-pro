import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wifiDisconnected02Icon({ iconName = "wifi-disconnected02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}