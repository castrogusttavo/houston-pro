import React from 'react'
import { Icon, IconProps } from '../Icon'

export function coordinate02Icon({ iconName = "coordinate02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}