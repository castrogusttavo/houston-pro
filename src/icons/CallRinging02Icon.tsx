import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallRinging02Icon({
  iconName = 'call-ringing02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
