import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowDown01Icon({ iconName = "square-arrow-down01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}