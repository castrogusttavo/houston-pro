import React from 'react';
import { Icon, IconProps } from '../Icon';

export const airplane-landing01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="airplane-landing01icon" />
);