import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailValidation02Icon({ iconName = "mail-validation02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}