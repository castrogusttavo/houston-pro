import React from 'react';
import { Icon, IconProps } from '../Icon';

export const not-equal-sign-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="not-equal-sign-squareicon" />
);