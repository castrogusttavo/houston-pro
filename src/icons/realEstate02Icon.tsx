import React from 'react'
import { Icon, IconProps } from '../Icon'

export function realEstate02Icon({ iconName = "real-estate02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}