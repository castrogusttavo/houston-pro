import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-down03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-down03icon" />
);