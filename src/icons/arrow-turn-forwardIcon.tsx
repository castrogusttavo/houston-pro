import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-turn-forwardIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-turn-forwardicon" />
);