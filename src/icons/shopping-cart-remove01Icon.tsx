import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-remove01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-remove01icon" />
);