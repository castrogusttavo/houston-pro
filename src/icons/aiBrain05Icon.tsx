import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiBrain05Icon({ iconName = "ai-brain05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}