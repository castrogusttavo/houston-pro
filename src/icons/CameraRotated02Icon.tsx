import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraRotated02Icon({
  iconName = 'camera-rotated02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
