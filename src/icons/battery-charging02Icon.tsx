import React from 'react';
import { Icon, IconProps } from '../Icon';

export const battery-charging02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="battery-charging02icon" />
);