import React from 'react'
import { Icon, IconProps } from '../Icon'

export function virtualRealityVr01Icon({ iconName = "virtual-reality-vr01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}