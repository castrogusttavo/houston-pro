import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight05Icon({ iconName = "rotate-right05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}