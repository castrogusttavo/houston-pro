import React from 'react';
import { Icon, IconProps } from '../Icon';

export const love-korean-fingerIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="love-korean-fingericon" />
);