import React from 'react'
import { Icon, IconProps } from '../Icon'

export function kickstarter02Icon({ iconName = "kickstarter02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}