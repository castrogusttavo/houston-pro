import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Download05Icon({
  iconName = 'download05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
