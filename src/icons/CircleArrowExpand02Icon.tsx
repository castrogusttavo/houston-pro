import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowExpand02Icon({
  iconName = 'circle-arrow-expand02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
