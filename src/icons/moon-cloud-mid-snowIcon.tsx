import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-mid-snowIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-mid-snowicon" />
);