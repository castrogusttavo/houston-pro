import React from 'react';
import { Icon, IconProps } from '../Icon';

export const auto-conversationsIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="auto-conversationsicon" />
);