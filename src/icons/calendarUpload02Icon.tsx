import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarUpload02Icon({ iconName = "calendar-upload02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}