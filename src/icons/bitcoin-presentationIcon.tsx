import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bitcoin-presentationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bitcoin-presentationicon" />
);