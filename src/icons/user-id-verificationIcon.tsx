import React from 'react';
import { Icon, IconProps } from '../Icon';

export const user-id-verificationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="user-id-verificationicon" />
);