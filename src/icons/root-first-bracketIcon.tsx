import React from 'react';
import { Icon, IconProps } from '../Icon';

export const root-first-bracketIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="root-first-bracketicon" />
);