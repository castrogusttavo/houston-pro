import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PieChart04Icon({
  iconName = 'pie-chart-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
