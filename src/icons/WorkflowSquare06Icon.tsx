import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare06Icon({
  iconName = 'workflow-square06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
