import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlusMinusCircle01Icon({ iconName = "plus-minus-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}