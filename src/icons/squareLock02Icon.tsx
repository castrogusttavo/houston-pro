import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareLock02Icon({ iconName = "square-lock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}