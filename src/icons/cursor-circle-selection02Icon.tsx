import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-circle-selection02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-circle-selection02icon" />
);