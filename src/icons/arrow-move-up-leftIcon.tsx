import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-move-up-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-move-up-lefticon" />
);