import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GoForward60SecIcon({ iconName = "go-forward60-sec", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}