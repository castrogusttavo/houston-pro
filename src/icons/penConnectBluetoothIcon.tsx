import React from 'react'
import { Icon, IconProps } from '../Icon'

export function penConnectBluetoothIcon({ iconName = "pen-connect-bluetooth", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}