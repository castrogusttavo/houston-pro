import React from 'react';
import { Icon, IconProps } from '../Icon';

export const smart-phone-landscapeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="smart-phone-landscapeicon" />
);