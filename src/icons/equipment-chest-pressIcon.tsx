import React from 'react';
import { Icon, IconProps } from '../Icon';

export const equipment-chest-pressIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="equipment-chest-pressicon" />
);