import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chemistry03Icon({ iconName = "chemistry03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}