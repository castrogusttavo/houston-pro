import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dialpadCircle01Icon({ iconName = "dialpad-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}