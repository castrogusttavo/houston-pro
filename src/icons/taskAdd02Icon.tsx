import React from 'react'
import { Icon, IconProps } from '../Icon'

export function taskAdd02Icon({ iconName = "task-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}