import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare01Icon({
  iconName = 'workflow-square01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
