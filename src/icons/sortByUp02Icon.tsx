import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sortByUp02Icon({ iconName = "sort-by-up02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}