import React from 'react'
import { Icon, IconProps } from '../Icon'

export function walletRemove01Icon({ iconName = "wallet-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}