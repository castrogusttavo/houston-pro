import React from 'react';
import { Icon, IconProps } from '../Icon';

export const ease-curve-control-pointsIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="ease-curve-control-pointsicon" />
);