import React from 'react';
import { Icon, IconProps } from '../Icon';

export const new-twitter-ellipseIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="new-twitter-ellipseicon" />
);