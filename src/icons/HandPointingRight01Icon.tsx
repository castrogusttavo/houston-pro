import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingRight01Icon({
  iconName = 'hand-pointing-right01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
