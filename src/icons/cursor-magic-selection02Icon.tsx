import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-magic-selection02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-magic-selection02icon" />
);