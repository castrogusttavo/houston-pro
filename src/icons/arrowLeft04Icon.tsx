import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowLeft04Icon({ iconName = "arrow-left04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}