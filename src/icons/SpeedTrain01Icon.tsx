import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SpeedTrain01Icon({
  iconName = 'speed-train01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
