import React from 'react'
import { Icon, IconProps } from '../Icon'

export function waterfallDown03Icon({ iconName = "waterfall-down03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}