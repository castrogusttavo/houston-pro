import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AutomotiveBattery01Icon({ iconName = "automotive-battery01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}