import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyBag02Icon({ iconName = "money-bag02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}