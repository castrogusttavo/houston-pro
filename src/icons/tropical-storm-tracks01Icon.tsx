import React from 'react';
import { Icon, IconProps } from '../Icon';

export const tropical-storm-tracks01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="tropical-storm-tracks01icon" />
);