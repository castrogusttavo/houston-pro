import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-offline03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-offline03icon" />
);