import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PowerSocket02Icon({ iconName = "power-socket02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}