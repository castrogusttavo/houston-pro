import React from 'react';
import { Icon, IconProps } from '../Icon';

export const approximately-equalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="approximately-equalicon" />
);