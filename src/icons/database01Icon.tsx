import React from 'react'
import { Icon, IconProps } from '../Icon'

export function database01Icon({ iconName = "database01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}