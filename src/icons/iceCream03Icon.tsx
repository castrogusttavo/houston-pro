import React from 'react'
import { Icon, IconProps } from '../Icon'

export function iceCream03Icon({ iconName = "ice-cream03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}