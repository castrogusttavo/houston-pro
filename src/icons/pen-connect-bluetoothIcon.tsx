import React from 'react';
import { Icon, IconProps } from '../Icon';

export const pen-connect-bluetoothIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="pen-connect-bluetoothicon" />
);