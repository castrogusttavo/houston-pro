import React from 'react';
import { Icon, IconProps } from '../Icon';

export const root-second-bracketIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="root-second-bracketicon" />
);