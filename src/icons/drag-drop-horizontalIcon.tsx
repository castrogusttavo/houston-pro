import React from 'react';
import { Icon, IconProps } from '../Icon';

export const drag-drop-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="drag-drop-horizontalicon" />
);