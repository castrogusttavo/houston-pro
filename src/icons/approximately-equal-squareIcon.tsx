import React from 'react';
import { Icon, IconProps } from '../Icon';

export const approximately-equal-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="approximately-equal-squareicon" />
);