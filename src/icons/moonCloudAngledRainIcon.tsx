import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudAngledRainIcon({ iconName = "moon-cloud-angled-rain", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}