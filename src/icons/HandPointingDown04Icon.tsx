import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingDown04Icon({
  iconName = 'hand-pointing-down04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
