import React from 'react'
import { Icon, IconProps } from '../Icon'

export function carParking02Icon({ iconName = "car-parking02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}