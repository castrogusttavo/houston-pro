import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TropicalStormTracks01Icon({
  iconName = 'tropical-storm-tracks01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
