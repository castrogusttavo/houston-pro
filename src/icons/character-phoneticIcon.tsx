import React from 'react';
import { Icon, IconProps } from '../Icon';

export const character-phoneticIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="character-phoneticicon" />
);