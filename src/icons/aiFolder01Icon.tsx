import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiFolder01Icon({ iconName = "ai-folder01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}