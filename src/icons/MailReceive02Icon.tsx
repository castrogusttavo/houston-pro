import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailReceive02Icon({ iconName = "mail-receive02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}