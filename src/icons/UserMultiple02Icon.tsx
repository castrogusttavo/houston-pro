import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserMultiple02Icon({
  iconName = 'user-multiple02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
