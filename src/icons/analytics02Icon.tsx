import React from 'react'
import { Icon, IconProps } from '../Icon'

export function analytics02Icon({ iconName = "analytics02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}