import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cloud-angled-rain-zapIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cloud-angled-rain-zapicon" />
);