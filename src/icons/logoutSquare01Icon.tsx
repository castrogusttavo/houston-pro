import React from 'react'
import { Icon, IconProps } from '../Icon'

export function logoutSquare01Icon({ iconName = "logout-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}