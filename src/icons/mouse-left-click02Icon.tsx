import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mouse-left-click02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mouse-left-click02icon" />
);