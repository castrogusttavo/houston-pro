import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-move-right-downIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-move-right-downicon" />
);