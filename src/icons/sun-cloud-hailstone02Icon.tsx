import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-hailstone02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-hailstone02icon" />
);