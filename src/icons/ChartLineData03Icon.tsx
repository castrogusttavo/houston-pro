import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ChartLineData03Icon({
  iconName = 'chart-line-data03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
