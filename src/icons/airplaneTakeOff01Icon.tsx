import React from 'react'
import { Icon, IconProps } from '../Icon'

export function airplaneTakeOff01Icon({ iconName = "airplane-take-off01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}