import React from 'react'
import { Icon, IconProps } from '../Icon'

export function webDesign01Icon({ iconName = "web-design01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}