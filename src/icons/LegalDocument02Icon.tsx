import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LegalDocument02Icon({
  iconName = 'legal-document02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
