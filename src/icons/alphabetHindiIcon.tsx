import React from 'react'
import { Icon, IconProps } from '../Icon'

export function alphabetHindiIcon({ iconName = "alphabet-hindi", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}