import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingRight07Icon({ iconName = "pointing-right07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}