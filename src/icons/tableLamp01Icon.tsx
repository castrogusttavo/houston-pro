import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tableLamp01Icon({ iconName = "table-lamp01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}