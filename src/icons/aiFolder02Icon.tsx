import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiFolder02Icon({ iconName = "ai-folder02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}