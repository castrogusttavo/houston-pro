import React from 'react';
import { Icon, IconProps } from '../Icon';

export const more-horizontal-circle01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="more-horizontal-circle01icon" />
);