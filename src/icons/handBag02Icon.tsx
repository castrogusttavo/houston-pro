import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handBag02Icon({ iconName = "hand-bag02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}