import React from 'react';
import { Icon, IconProps } from '../Icon';

export const medal-second-placeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="medal-second-placeicon" />
);