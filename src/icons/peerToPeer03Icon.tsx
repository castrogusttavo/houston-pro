import React from 'react'
import { Icon, IconProps } from '../Icon'

export function peerToPeer03Icon({ iconName = "peer-to-peer03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}