import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrange-by-numbers91Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrange-by-numbers91icon" />
);