import React from 'react';
import { Icon, IconProps } from '../Icon';

export const central-shaheed-minarIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="central-shaheed-minaricon" />
);