import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationRemove01Icon({ iconName = "location-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}