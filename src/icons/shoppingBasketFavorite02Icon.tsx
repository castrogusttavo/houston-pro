import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketFavorite02Icon({ iconName = "shopping-basket-favorite02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}