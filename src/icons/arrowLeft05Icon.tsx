import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowLeft05Icon({ iconName = "arrow-left05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}