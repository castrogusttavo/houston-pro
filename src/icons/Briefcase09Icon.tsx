import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase09Icon({
  iconName = 'briefcase09',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
