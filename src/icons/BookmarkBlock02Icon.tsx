import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkBlock02Icon({ iconName = "bookmark-block02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}