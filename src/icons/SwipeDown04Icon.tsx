import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown04Icon({ iconName = "swipe-down04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}