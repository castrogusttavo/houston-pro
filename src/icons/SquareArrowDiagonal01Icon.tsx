import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDiagonal01Icon({
  iconName = 'square-arrow-diagonal01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
