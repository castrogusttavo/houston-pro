import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashboardCircleSettingsIcon({ iconName = "dashboard-circle-settings", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}