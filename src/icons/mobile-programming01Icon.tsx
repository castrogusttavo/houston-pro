import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mobile-programming01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mobile-programming01icon" />
);