import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailRemove02Icon({ iconName = "mail-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}