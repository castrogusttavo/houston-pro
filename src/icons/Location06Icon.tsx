import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location06Icon({
  iconName = 'location06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
