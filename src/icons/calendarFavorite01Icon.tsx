import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarFavorite01Icon({ iconName = "calendar-favorite01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}