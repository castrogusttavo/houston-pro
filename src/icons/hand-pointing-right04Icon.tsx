import React from 'react';
import { Icon, IconProps } from '../Icon';

export const hand-pointing-right04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="hand-pointing-right04icon" />
);