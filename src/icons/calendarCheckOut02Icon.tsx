import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarCheckOut02Icon({ iconName = "calendar-check-out02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}