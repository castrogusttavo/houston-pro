import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingCartCheck01Icon({ iconName = "shopping-cart-check01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}