import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PinLocation02Icon({
  iconName = 'pin-location02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
