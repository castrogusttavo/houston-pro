import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentation04Icon({ iconName = "presentation04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}