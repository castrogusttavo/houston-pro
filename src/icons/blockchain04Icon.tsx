import React from 'react'
import { Icon, IconProps } from '../Icon'

export function blockchain04Icon({ iconName = "blockchain04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}