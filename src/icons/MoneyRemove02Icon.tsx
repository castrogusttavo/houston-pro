import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyRemove02Icon({ iconName = "money-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}