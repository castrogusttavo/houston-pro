import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pinLocation01Icon({ iconName = "pin-location01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}