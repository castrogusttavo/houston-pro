import React from 'react'
import { Icon, IconProps } from '../Icon'

export function twoFinger03Icon({ iconName = "two-finger03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}