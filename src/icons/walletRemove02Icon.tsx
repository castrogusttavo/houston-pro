import React from 'react'
import { Icon, IconProps } from '../Icon'

export function walletRemove02Icon({ iconName = "wallet-remove02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}