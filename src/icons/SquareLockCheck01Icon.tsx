import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockCheck01Icon({ iconName = "square-lock-check01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}