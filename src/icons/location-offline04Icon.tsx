import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-offline04Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-offline04icon" />
);