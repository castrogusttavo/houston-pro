import React from 'react';
import { Icon, IconProps } from '../Icon';

export const thumbs-down-rectangleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="thumbs-down-rectangleicon" />
);