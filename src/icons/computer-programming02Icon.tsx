import React from 'react';
import { Icon, IconProps } from '../Icon';

export const computer-programming02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="computer-programming02icon" />
);