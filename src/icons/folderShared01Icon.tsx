import React from 'react'
import { Icon, IconProps } from '../Icon'

export function folderShared01Icon({ iconName = "folder-shared01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}