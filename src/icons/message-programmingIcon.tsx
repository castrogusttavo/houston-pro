import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-programmingIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-programmingicon" />
);