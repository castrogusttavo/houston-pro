import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarMinus02Icon({ iconName = "calendar-minus02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}