import React from 'react'
import { Icon, IconProps } from '../Icon'

export function peerToPeer01Icon({ iconName = "peer-to-peer01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}