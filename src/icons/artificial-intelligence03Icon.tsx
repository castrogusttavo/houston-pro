import React from 'react';
import { Icon, IconProps } from '../Icon';

export const artificial-intelligence03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="artificial-intelligence03icon" />
);