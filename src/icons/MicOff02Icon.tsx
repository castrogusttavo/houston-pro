import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MicOff02Icon({ iconName = 'mic-off02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
