import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowLeft03Icon({ iconName = "arrow-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}