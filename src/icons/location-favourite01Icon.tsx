import React from 'react';
import { Icon, IconProps } from '../Icon';

export const location-favourite01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="location-favourite01icon" />
);