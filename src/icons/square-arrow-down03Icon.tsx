import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-down03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-down03icon" />
);