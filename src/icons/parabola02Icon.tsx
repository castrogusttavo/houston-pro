import React from 'react'
import { Icon, IconProps } from '../Icon'

export function parabola02Icon({ iconName = "parabola02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}