import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowDownRight02Icon({ iconName = "arrow-down-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}