import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-data-transfer-horizontalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-data-transfer-horizontalicon" />
);