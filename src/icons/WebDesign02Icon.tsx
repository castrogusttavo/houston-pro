import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WebDesign02Icon({ iconName = "web-design02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}