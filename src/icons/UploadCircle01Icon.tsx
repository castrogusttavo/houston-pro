import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UploadCircle01Icon({ iconName = "upload-circle01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}