import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreHorizontalSquare01Icon({ iconName = "more-horizontal-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}