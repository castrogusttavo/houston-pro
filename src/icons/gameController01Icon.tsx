import React from 'react'
import { Icon, IconProps } from '../Icon'

export function gameController01Icon({ iconName = "game-controller01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}