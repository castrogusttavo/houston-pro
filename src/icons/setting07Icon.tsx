import React from 'react'
import { Icon, IconProps } from '../Icon'

export function setting07Icon({ iconName = "setting07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}