import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-basket-check-in02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-basket-check-in02icon" />
);