import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Root3rdBracketIcon({
  iconName = 'root-3rd-bracket',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
