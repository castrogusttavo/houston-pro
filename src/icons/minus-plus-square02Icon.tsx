import React from 'react';
import { Icon, IconProps } from '../Icon';

export const minus-plus-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="minus-plus-square02icon" />
);