import React from 'react';
import { Icon, IconProps } from '../Icon';

export const equipment-bench-pressIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="equipment-bench-pressicon" />
);