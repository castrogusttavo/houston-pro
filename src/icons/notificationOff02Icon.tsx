import React from 'react'
import { Icon, IconProps } from '../Icon'

export function notificationOff02Icon({ iconName = "notification-off02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}