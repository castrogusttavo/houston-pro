import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBasketDone03Icon({ iconName = "shopping-basket-done03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}