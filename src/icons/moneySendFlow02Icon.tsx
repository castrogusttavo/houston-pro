import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneySendFlow02Icon({ iconName = "money-send-flow02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}