import React from 'react'
import { Icon, IconProps } from '../Icon'

export function medalSecondPlaceIcon({ iconName = "medal-second-place", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}