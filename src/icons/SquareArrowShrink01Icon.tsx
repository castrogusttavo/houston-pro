import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowShrink01Icon({
  iconName = 'square-arrow-shrink01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
