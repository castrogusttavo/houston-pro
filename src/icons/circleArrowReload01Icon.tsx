import React from 'react'
import { Icon, IconProps } from '../Icon'

export function circleArrowReload01Icon({ iconName = "circle-arrow-reload01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}