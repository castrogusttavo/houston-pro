import React from 'react'
import { Icon, IconProps } from '../Icon'

export function accountSetting03Icon({ iconName = "account-setting03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}