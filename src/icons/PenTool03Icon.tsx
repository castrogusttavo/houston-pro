import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PenTool03Icon({
  iconName = 'pen-tool03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
