import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortByUp01Icon({
  iconName = 'sort-by-up01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
