import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cancel-circle-half-dotIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cancel-circle-half-doticon" />
);