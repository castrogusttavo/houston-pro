import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationShare01Icon({ iconName = "location-share01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}