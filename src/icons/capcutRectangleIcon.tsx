import React from 'react'
import { Icon, IconProps } from '../Icon'

export function capcutRectangleIcon({ iconName = "capcut-rectangle", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}