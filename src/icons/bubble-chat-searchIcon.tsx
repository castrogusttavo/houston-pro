import React from 'react';
import { Icon, IconProps } from '../Icon';

export const bubble-chat-searchIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="bubble-chat-searchicon" />
);