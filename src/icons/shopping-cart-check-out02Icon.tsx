import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-cart-check-out02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-cart-check-out02icon" />
);