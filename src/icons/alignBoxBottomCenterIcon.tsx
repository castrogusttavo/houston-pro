import React from 'react'
import { Icon, IconProps } from '../Icon'

export function alignBoxBottomCenterIcon({ iconName = "align-box-bottom-center", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}