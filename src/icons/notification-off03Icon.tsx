import React from 'react';
import { Icon, IconProps } from '../Icon';

export const notification-off03Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="notification-off03icon" />
);