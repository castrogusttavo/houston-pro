import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeLeft09Icon({ iconName = "swipe-left09", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}