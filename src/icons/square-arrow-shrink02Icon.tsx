import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-shrink02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-shrink02icon" />
);