import React from 'react'
import { Icon, IconProps } from '../Icon'

export function invoice02Icon({ iconName = "invoice02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}