import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WalletRemove01Icon({
  iconName = 'wallet-remove01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
