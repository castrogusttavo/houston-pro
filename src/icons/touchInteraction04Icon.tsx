import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchInteraction04Icon({ iconName = "touch-interaction04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}