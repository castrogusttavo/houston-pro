import React from 'react';
import { Icon, IconProps } from '../Icon';

export const tablet-connected-usbIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="tablet-connected-usbicon" />
);