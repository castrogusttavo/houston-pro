import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailUnlock02Icon({ iconName = "mail-unlock02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}