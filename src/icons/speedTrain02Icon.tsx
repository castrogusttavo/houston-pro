import React from 'react'
import { Icon, IconProps } from '../Icon'

export function speedTrain02Icon({ iconName = "speed-train02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}