import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationCheck02Icon({ iconName = "location-check02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}