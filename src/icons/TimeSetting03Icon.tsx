import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TimeSetting03Icon({ iconName = "time-setting03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}