import React from 'react';
import { Icon, IconProps } from '../Icon';

export const curvy-up-down-directionIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="curvy-up-down-directionicon" />
);