import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp03Icon({ iconName = "swipe-up03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}