import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Calendar04Icon({
  iconName = 'calendar04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
