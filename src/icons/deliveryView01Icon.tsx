import React from 'react'
import { Icon, IconProps } from '../Icon'

export function deliveryView01Icon({ iconName = "delivery-view01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}