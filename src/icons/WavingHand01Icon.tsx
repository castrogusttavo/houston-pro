import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WavingHand01Icon({ iconName = "waving-hand01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}