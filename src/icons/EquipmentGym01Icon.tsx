import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EquipmentGym01Icon({
  iconName = 'equipment-gym01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
