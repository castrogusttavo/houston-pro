import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messageSearch02Icon({ iconName = "message-search02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}