import React from 'react'
import { Icon, IconProps } from '../Icon'

export function locationUser03Icon({ iconName = "location-user03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}