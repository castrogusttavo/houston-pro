import React from 'react';
import { Icon, IconProps } from '../Icon';

export const text-variable-frontIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="text-variable-fronticon" />
);