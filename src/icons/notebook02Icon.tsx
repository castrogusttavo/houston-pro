import React from 'react'
import { Icon, IconProps } from '../Icon'

export function notebook02Icon({ iconName = "notebook02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}