import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Trolley02Icon({ iconName = 'trolley02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
