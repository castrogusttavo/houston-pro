import React from 'react';
import { Icon, IconProps } from '../Icon';

export function UserGroup03Icon({
  iconName = 'user-group-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
