import React from 'react'
import { Icon, IconProps } from '../Icon'

export function notificationSnooze01Icon({ iconName = "notification-snooze01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}