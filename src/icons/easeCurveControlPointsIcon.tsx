import React from 'react'
import { Icon, IconProps } from '../Icon'

export function easeCurveControlPointsIcon({ iconName = "ease-curve-control-points", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}