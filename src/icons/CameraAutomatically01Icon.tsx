import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraAutomatically01Icon({
  iconName = 'camera-automatically01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
