import React from 'react'
import { Icon, IconProps } from '../Icon'

export function messagePreview02Icon({ iconName = "message-preview02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}