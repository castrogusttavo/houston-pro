import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowMoveUpRightIcon({ iconName = "arrow-move-up-right", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}