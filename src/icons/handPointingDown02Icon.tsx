import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingDown02Icon({ iconName = "hand-pointing-down02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}