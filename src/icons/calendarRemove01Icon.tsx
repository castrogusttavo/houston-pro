import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarRemove01Icon({ iconName = "calendar-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}