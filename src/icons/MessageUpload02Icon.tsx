import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageUpload02Icon({ iconName = "message-upload02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}