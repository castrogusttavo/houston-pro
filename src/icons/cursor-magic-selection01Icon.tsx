import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-magic-selection01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-magic-selection01icon" />
);