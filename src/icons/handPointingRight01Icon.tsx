import React from 'react'
import { Icon, IconProps } from '../Icon'

export function handPointingRight01Icon({ iconName = "hand-pointing-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}