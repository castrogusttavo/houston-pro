import React from 'react';
import { Icon, IconProps } from '../Icon';

export const camera-automatically02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="camera-automatically02icon" />
);