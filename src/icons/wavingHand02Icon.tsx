import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wavingHand02Icon({ iconName = "waving-hand02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}