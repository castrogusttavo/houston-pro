import React from 'react'
import { Icon, IconProps } from '../Icon'

export function aiBrain01Icon({ iconName = "ai-brain01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}