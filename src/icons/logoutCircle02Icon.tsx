import React from 'react'
import { Icon, IconProps } from '../Icon'

export function logoutCircle02Icon({ iconName = "logout-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}