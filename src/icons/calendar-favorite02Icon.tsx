import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-favorite02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-favorite02icon" />
);