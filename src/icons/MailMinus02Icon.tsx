import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailMinus02Icon({ iconName = "mail-minus02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}