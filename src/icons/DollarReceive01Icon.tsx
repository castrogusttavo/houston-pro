import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DollarReceive01Icon({ iconName = "dollar-receive01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}