import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowDiagonal02Icon({ iconName = "square-arrow-diagonal02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}