import React from 'react'
import { Icon, IconProps } from '../Icon'

export function smartPhone02Icon({ iconName = "smart-phone02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}