import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Exchange02Icon({ iconName = "exchange02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}