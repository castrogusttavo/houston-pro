import React from 'react';
import { Icon, IconProps } from '../Icon';

export const credit-card-validationIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="credit-card-validationicon" />
);