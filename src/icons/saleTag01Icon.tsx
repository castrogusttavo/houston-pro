import React from 'react'
import { Icon, IconProps } from '../Icon'

export function saleTag01Icon({ iconName = "sale-tag01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}