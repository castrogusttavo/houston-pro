import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wifiConnected02Icon({ iconName = "wifi-connected02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}