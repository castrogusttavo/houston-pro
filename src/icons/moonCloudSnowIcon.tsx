import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moonCloudSnowIcon({ iconName = "moon-cloud-snow", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}