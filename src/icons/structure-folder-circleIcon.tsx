import React from 'react';
import { Icon, IconProps } from '../Icon';

export const structure-folder-circleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="structure-folder-circleicon" />
);