import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowUpRight02Icon({ iconName = "arrow-up-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}