import React from 'react';
import { Icon, IconProps } from '../Icon';

export const message-download01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="message-download01icon" />
);