import React from 'react';
import { Icon, IconProps } from '../Icon';

export const finger-print-minusIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="finger-print-minusicon" />
);