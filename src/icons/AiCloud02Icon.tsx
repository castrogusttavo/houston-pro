import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiCloud02Icon({
  iconName = 'ai-cloud02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
