import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cloud-saving-done02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cloud-saving-done02icon" />
);