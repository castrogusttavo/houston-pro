import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sailboatOffshoreIcon({ iconName = "sailboat-offshore", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}