import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dragLeft03Icon({ iconName = "drag-left03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}