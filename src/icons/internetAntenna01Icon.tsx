import React from 'react'
import { Icon, IconProps } from '../Icon'

export function internetAntenna01Icon({ iconName = "internet-antenna01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}