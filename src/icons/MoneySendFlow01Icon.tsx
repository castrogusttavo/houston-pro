import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneySendFlow01Icon({ iconName = "money-send-flow01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}