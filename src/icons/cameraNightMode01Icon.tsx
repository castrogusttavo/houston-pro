import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraNightMode01Icon({ iconName = "camera-night-mode01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}