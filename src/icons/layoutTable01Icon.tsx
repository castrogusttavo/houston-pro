import React from 'react'
import { Icon, IconProps } from '../Icon'

export function layoutTable01Icon({ iconName = "layout-table01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}