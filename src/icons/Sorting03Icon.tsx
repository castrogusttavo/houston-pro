import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting03Icon({ iconName = "sorting03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}