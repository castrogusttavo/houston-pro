import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingCart02Icon({ iconName = "shopping-cart02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}