import React from 'react'
import { Icon, IconProps } from '../Icon'

export function inequalitySquare02Icon({ iconName = "inequality-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}