import React from 'react'
import { Icon, IconProps } from '../Icon'

export function basketball02Icon({ iconName = "basketball02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}