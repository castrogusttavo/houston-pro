import React from 'react';
import { Icon, IconProps } from '../Icon';

export const music-note-square02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="music-note-square02icon" />
);