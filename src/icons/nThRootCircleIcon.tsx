import React from 'react'
import { Icon, IconProps } from '../Icon'

export function nThRootCircleIcon({ iconName = "n-th-root-circle", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}