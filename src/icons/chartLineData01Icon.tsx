import React from 'react'
import { Icon, IconProps } from '../Icon'

export function chartLineData01Icon({ iconName = "chart-line-data01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}