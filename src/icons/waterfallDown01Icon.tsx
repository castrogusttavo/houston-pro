import React from 'react'
import { Icon, IconProps } from '../Icon'

export function waterfallDown01Icon({ iconName = "waterfall-down01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}