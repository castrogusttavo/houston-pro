import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShippingTruck02Icon({
  iconName = 'shipping-truck02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
