import React from 'react'
import { Icon, IconProps } from '../Icon'

export function hierarchySquare07Icon({ iconName = "hierarchy-square07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}