import React from 'react'
import { Icon, IconProps } from '../Icon'

export function internetAntenna03Icon({ iconName = "internet-antenna03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}