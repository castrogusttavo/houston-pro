import React from 'react';
import { Icon, IconProps } from '../Icon';

export const distribute-horizontal-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="distribute-horizontal-lefticon" />
);