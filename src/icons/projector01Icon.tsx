import React from 'react'
import { Icon, IconProps } from '../Icon'

export function projector01Icon({ iconName = "projector01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}