import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AlbumNotFound02Icon({
  iconName = 'album-not-found02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
