import React from 'react'
import { Icon, IconProps } from '../Icon'

export function keyframesDoubleAddIcon({ iconName = "keyframes-double-add", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}