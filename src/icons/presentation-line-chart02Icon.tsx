import React from 'react';
import { Icon, IconProps } from '../Icon';

export const presentation-line-chart02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="presentation-line-chart02icon" />
);