import React from 'react'
import { Icon, IconProps } from '../Icon'

export function agreement01Icon({ iconName = "agreement01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}