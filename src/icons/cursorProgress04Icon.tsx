import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cursorProgress04Icon({ iconName = "cursor-progress04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}