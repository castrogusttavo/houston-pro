import React from 'react'
import { Icon, IconProps } from '../Icon'

export function loginSquare01Icon({ iconName = "login-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}