import React from 'react'
import { Icon, IconProps } from '../Icon'

export function borderTop01Icon({ iconName = "border-top01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}