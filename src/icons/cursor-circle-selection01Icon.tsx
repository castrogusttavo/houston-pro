import React from 'react';
import { Icon, IconProps } from '../Icon';

export const cursor-circle-selection01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="cursor-circle-selection01icon" />
);