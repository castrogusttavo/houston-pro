import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft07Icon({
  iconName = 'swipe-left07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
