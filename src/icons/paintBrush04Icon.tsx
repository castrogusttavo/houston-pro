import React from 'react'
import { Icon, IconProps } from '../Icon'

export function paintBrush04Icon({ iconName = "paint-brush04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}