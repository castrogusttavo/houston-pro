import React from 'react'
import { Icon, IconProps } from '../Icon'

export function gameController02Icon({ iconName = "game-controller02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}