import React from 'react';
import { Icon, IconProps } from '../Icon';

export const keyframes-multiple-removeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="keyframes-multiple-removeicon" />
);