import React from 'react'
import { Icon, IconProps } from '../Icon'

export function audioBook02Icon({ iconName = "audio-book02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}