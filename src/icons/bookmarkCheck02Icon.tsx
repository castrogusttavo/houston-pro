import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bookmarkCheck02Icon({ iconName = "bookmark-check02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}