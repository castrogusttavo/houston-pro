import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ComingSoon02Icon({
  iconName = 'coming-soon02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
