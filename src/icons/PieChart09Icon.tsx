import React from 'react';
import { Icon, IconProps } from '../Icon';

export function PieChart09Icon({
  iconName = 'pie-chart-09',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
