import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-up-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-up-lefticon" />
);