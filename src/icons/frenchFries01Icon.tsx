import React from 'react'
import { Icon, IconProps } from '../Icon'

export function frenchFries01Icon({ iconName = "french-fries01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}