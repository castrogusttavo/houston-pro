import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreVerticalSquare02Icon({ iconName = "more-vertical-square02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}