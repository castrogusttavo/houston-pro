import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraAutomatically01Icon({ iconName = "camera-automatically01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}