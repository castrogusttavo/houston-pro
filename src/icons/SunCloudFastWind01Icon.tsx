import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudFastWind01Icon({ iconName = "sun-cloud-fast-wind01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}