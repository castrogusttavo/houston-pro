import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft05Icon({ iconName = "pointing-left05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}