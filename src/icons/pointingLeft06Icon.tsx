import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft06Icon({ iconName = "pointing-left06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}