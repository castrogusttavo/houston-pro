import React from 'react';
import { Icon, IconProps } from '../Icon';

export const preference-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="preference-verticalicon" />
);