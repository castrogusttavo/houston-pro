import React from 'react'
import { Icon, IconProps } from '../Icon'

export function lockSync02Icon({ iconName = "lock-sync02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}