import React from 'react'
import { Icon, IconProps } from '../Icon'

export function solarPanel01Icon({ iconName = "solar-panel01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}