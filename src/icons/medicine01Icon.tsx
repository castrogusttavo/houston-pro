import React from 'react'
import { Icon, IconProps } from '../Icon'

export function medicine01Icon({ iconName = "medicine01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}