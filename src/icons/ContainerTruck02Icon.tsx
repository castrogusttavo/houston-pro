import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ContainerTruck02Icon({ iconName = "container-truck02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}