import React from 'react'
import { Icon, IconProps } from '../Icon'

export function stickyNote02Icon({ iconName = "sticky-note02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}