import React from 'react';
import { Icon, IconProps } from '../Icon';

export const credit-card-freezeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="credit-card-freezeicon" />
);