import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mortarboard01Icon({ iconName = "mortarboard01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}