import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailLove01Icon({ iconName = "mail-love01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}