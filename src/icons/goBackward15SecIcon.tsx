import React from 'react'
import { Icon, IconProps } from '../Icon'

export function goBackward15SecIcon({ iconName = "go-backward15-sec", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}