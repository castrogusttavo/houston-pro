import React from 'react';
import { Icon, IconProps } from '../Icon';

export const mobile-programming02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="mobile-programming02icon" />
);