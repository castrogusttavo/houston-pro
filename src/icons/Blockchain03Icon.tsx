import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Blockchain03Icon({ iconName = "blockchain03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}