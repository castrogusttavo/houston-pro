import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cameraAdd02Icon({ iconName = "camera-add02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}