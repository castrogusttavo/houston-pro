import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SafetyPin02Icon({ iconName = "safety-pin02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}