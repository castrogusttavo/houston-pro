import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mobileProgramming02Icon({ iconName = "mobile-programming02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}