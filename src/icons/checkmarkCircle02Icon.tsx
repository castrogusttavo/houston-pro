import React from 'react'
import { Icon, IconProps } from '../Icon'

export function checkmarkCircle02Icon({ iconName = "checkmark-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}