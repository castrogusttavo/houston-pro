import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUpLeft02Icon({
  iconName = 'arrow-up-left02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
