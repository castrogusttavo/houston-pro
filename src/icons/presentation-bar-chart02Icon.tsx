import React from 'react';
import { Icon, IconProps } from '../Icon';

export const presentation-bar-chart02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="presentation-bar-chart02icon" />
);