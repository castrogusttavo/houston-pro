import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mailAccount01Icon({ iconName = "mail-account01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}