import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pointingLeft07Icon({ iconName = "pointing-left07", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}