import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowShrink02Icon({
  iconName = 'arrow-shrink02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
