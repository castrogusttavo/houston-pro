import React from 'react'
import { Icon, IconProps } from '../Icon'

export function textAlignRight01Icon({ iconName = "text-align-right01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}