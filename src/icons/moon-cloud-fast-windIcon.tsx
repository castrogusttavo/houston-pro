import React from 'react';
import { Icon, IconProps } from '../Icon';

export const moon-cloud-fast-windIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="moon-cloud-fast-windicon" />
);