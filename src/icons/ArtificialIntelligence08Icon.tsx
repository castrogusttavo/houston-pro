import React from 'react';
import { Icon, IconProps } from '../Icon';

export function ArtificialIntelligence08Icon({
  iconName = 'artificial-intelligence-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
