import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchInteraction02Icon({ iconName = "touch-interaction02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}