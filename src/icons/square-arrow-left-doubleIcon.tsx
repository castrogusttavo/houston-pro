import React from 'react';
import { Icon, IconProps } from '../Icon';

export const square-arrow-left-doubleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="square-arrow-left-doubleicon" />
);