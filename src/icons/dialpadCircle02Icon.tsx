import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dialpadCircle02Icon({ iconName = "dialpad-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}