import React from 'react';
import { Icon, IconProps } from '../Icon';

export const first-bracket-squareIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="first-bracket-squareicon" />
);