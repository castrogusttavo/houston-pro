import React from 'react';
import { Icon, IconProps } from '../Icon';

export const shopping-bag-removeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="shopping-bag-removeicon" />
);