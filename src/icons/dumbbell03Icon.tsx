import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dumbbell03Icon({ iconName = "dumbbell03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}