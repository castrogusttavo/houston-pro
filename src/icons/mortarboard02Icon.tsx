import React from 'react'
import { Icon, IconProps } from '../Icon'

export function mortarboard02Icon({ iconName = "mortarboard02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}