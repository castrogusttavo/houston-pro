import React from 'react'
import { Icon, IconProps } from '../Icon'

export function playlist02Icon({ iconName = "playlist02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}