import React from 'react'
import { Icon, IconProps } from '../Icon'

export function layerMask01Icon({ iconName = "layer-mask01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}