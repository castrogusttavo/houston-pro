import React from 'react'
import { Icon, IconProps } from '../Icon'

export function tropicalStormTracks01Icon({ iconName = "tropical-storm-tracks01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}