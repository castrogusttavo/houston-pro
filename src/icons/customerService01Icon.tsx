import React from 'react'
import { Icon, IconProps } from '../Icon'

export function customerService01Icon({ iconName = "customer-service01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}