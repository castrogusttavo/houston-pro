import React from 'react'
import { Icon, IconProps } from '../Icon'

export function dashboardSquare03Icon({ iconName = "dashboard-square03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}