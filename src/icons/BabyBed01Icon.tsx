import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BabyBed01Icon({ iconName = "baby-bed01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}