import React from 'react'
import { Icon, IconProps } from '../Icon'

export function keyframeAlignHorizontalIcon({ iconName = "keyframe-align-horizontal", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}