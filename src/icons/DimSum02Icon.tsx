import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DimSum02Icon({ iconName = 'dim-sum02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
