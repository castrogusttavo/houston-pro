import React from 'react';
import { Icon, IconProps } from '../Icon';

export const delivery-tracking02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="delivery-tracking02icon" />
);