import React from 'react'
import { Icon, IconProps } from '../Icon'

export function touchpad03Icon({ iconName = "touchpad03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}