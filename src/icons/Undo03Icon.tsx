import React from 'react';
import { Icon, IconProps } from '../Icon';

export function Undo03Icon({
  iconName = 'undo-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />;
}
