import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarDownload02Icon({ iconName = "calendar-download02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}