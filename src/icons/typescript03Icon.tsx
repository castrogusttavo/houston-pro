import React from 'react'
import { Icon, IconProps } from '../Icon'

export function typescript03Icon({ iconName = "typescript03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}