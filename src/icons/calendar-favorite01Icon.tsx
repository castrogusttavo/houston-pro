import React from 'react';
import { Icon, IconProps } from '../Icon';

export const calendar-favorite01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="calendar-favorite01icon" />
);