import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowRight04Icon({ iconName = "arrow-right04", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}