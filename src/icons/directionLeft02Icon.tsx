import React from 'react'
import { Icon, IconProps } from '../Icon'

export function directionLeft02Icon({ iconName = "direction-left02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}