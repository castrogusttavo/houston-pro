import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeRight09Icon({ iconName = "swipe-right09", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}