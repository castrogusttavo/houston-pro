import React from 'react'
import { Icon, IconProps } from '../Icon'

export function pyramidStructure02Icon({ iconName = "pyramid-structure02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}