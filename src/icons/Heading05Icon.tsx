import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Heading05Icon({ iconName = "heading05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}