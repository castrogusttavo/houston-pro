import React from 'react'
import { Icon, IconProps } from '../Icon'

export function twoFinger05Icon({ iconName = "two-finger05", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}