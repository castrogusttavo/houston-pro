import React from 'react'
import { Icon, IconProps } from '../Icon'

export function notificationSnooze03Icon({ iconName = "notification-snooze03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}