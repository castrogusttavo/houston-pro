import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareLockMinus01Icon({ iconName = "square-lock-minus01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}