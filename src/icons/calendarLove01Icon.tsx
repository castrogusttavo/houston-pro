import React from 'react'
import { Icon, IconProps } from '../Icon'

export function calendarLove01Icon({ iconName = "calendar-love01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}