import React from 'react';
import { Icon, IconProps } from '../Icon';

export const inequality-square01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="inequality-square01icon" />
);