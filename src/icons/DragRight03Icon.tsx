import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragRight03Icon({ iconName = "drag-right03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}