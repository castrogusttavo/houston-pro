import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrangeByNumbers19Icon({ iconName = "arrange-by-numbers19", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}