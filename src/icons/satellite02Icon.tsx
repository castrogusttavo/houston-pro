import React from 'react'
import { Icon, IconProps } from '../Icon'

export function satellite02Icon({ iconName = "satellite02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}