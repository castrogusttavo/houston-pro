import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moneyRemove01Icon({ iconName = "money-remove01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}