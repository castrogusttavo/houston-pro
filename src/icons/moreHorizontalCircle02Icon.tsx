import React from 'react'
import { Icon, IconProps } from '../Icon'

export function moreHorizontalCircle02Icon({ iconName = "more-horizontal-circle02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}