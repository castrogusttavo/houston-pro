import React from 'react'
import { Icon, IconProps } from '../Icon'

export function bathtub02Icon({ iconName = "bathtub02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}