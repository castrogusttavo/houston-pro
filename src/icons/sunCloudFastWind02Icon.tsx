import React from 'react'
import { Icon, IconProps } from '../Icon'

export function sunCloudFastWind02Icon({ iconName = "sun-cloud-fast-wind02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}