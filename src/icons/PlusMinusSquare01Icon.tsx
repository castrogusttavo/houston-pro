import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlusMinusSquare01Icon({
  iconName = 'plus-minus-square01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
