import React from 'react'
import { Icon, IconProps } from '../Icon'

export function wpsOfficeRectangleIcon({ iconName = "wps-office-rectangle", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}