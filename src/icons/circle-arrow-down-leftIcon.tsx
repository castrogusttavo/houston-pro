import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-down-leftIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-down-lefticon" />
);