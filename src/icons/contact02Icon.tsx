import React from 'react'
import { Icon, IconProps } from '../Icon'

export function contact02Icon({ iconName = "contact02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}