import React from 'react';
import { Icon, IconProps } from '../Icon';

export const arrow-move-down-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="arrow-move-down-righticon" />
);