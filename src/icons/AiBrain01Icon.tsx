import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiBrain01Icon({
  iconName = 'ai-brain01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
