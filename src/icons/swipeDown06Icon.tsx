import React from 'react'
import { Icon, IconProps } from '../Icon'

export function swipeDown06Icon({ iconName = "swipe-down06", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}