import React from 'react';
import { Icon, IconProps } from '../Icon';

export const wifi-disconnected02Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="wifi-disconnected02icon" />
);