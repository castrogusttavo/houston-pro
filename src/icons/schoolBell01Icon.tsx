import React from 'react'
import { Icon, IconProps } from '../Icon'

export function schoolBell01Icon({ iconName = "school-bell01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}