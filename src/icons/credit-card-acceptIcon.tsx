import React from 'react';
import { Icon, IconProps } from '../Icon';

export const credit-card-acceptIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="credit-card-accepticon" />
);