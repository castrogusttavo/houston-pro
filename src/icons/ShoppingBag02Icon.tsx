import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBag02Icon({
  iconName = 'shopping-bag02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
