import React from 'react'
import { Icon, IconProps } from '../Icon'

export function layoutTable02Icon({ iconName = "layout-table02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}