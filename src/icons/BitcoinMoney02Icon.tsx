import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BitcoinMoney02Icon({ iconName = "bitcoin-money02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}