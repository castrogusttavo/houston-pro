import React from 'react';
import { Icon, IconProps } from '../Icon';

export const multiplication-signIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="multiplication-signicon" />
);