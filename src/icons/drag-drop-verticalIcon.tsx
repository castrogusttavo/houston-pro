import React from 'react';
import { Icon, IconProps } from '../Icon';

export const drag-drop-verticalIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="drag-drop-verticalicon" />
);