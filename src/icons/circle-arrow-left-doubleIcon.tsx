import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-left-doubleIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-left-doubleicon" />
);