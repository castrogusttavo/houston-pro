import React from 'react'
import { Icon, IconProps } from '../Icon'

export function squareArrowUp01Icon({ iconName = "square-arrow-up01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}