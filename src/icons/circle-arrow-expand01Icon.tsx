import React from 'react';
import { Icon, IconProps } from '../Icon';

export const circle-arrow-expand01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="circle-arrow-expand01icon" />
);