import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userStar02Icon({ iconName = "user-star02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}