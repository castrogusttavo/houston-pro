import React from 'react'
import { Icon, IconProps } from '../Icon'

export function arrowUp01Icon({ iconName = "arrow-up01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}