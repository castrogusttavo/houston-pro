import React from 'react'
import { Icon, IconProps } from '../Icon'

export function alignBoxMiddleCenterIcon({ iconName = "align-box-middle-center", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}