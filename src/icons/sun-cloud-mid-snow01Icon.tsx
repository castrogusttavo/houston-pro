import React from 'react';
import { Icon, IconProps } from '../Icon';

export const sun-cloud-mid-snow01Icon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="sun-cloud-mid-snow01icon" />
);