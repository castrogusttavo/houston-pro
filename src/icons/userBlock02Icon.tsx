import React from 'react'
import { Icon, IconProps } from '../Icon'

export function userBlock02Icon({ iconName = "user-block02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}