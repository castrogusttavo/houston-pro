import React from 'react'
import { Icon, IconProps } from '../Icon'

export function cylinder01Icon({ iconName = "cylinder01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}