import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FilmRoll01Icon({ iconName = "film-roll01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}