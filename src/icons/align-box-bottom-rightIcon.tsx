import React from 'react';
import { Icon, IconProps } from '../Icon';

export const align-box-bottom-rightIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="align-box-bottom-righticon" />
);