import React from 'react';
import { Icon, IconProps } from '../Icon';

export const screen-add-to-homeIcon: React.FC<IconProps> = (props) => (
  <Icon {...props} iconName="screen-add-to-homeicon" />
);