import React from 'react'
import { Icon, IconProps } from '../Icon'

export function shoppingBag03Icon({ iconName = "shopping-bag03", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}