import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArtificialIntelligence01Icon({
  iconName = 'artificial-intelligence01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
