import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallEnd02Icon({ iconName = "call-end02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}