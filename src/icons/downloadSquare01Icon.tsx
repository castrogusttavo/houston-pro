import React from 'react'
import { Icon, IconProps } from '../Icon'

export function downloadSquare01Icon({ iconName = "download-square01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}