import React from 'react'
import { Icon, IconProps } from '../Icon'

export function presentationBarChart02Icon({ iconName = "presentation-bar-chart02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}