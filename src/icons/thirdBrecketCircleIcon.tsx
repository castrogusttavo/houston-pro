import React from 'react'
import { Icon, IconProps } from '../Icon'

export function thirdBrecketCircleIcon({ iconName = "third-brecket-circle", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}