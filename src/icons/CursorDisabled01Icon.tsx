import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorDisabled01Icon({ iconName = "cursor-disabled01", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}