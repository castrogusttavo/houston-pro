import React from 'react'
import { Icon, IconProps } from '../Icon'

export function rotateRight02Icon({ iconName = "rotate-right02", ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}